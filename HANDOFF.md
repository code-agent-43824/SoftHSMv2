# HANDOFF — незавершённое прямо сейчас

Обновлено: 2026-08-12.

## В работе

Этап 5 — приведение репозитория к правилам `AGENTS.md`. Документы заведены и
закоммичены; содержательная часть этапа выполнена.

## Точный следующий шаг

Подтвердить у владельца этапы 6 и 7 в `docs/ROADMAP.md` — проверку профиля
`FAKE_RUTOKEN_ECP` на реальном ПО под Рутокен ECP и юнит-тесты на код
ГОСТ 2012. Оба записаны как понимание агента, владельцем ещё не согласованы;
порядок и состав могут измениться. До подтверждения новый этап не начинать.

## О чём знать следующему агенту

- Юнит-тесты не запускаются ни одним workflow, и это осознанное решение
  владельца. Правки в `src/lib/**/test/**` и в криптокоде проверяйте локально:
  `cmake -S . -B build && cmake --build build --parallel && ctest --test-dir build --output-on-failure`.
- Запуск `ci.yml` на вершине `006c944` отменён; вершина подтверждена только
  успешным прогоном `portable-release.yml`.
