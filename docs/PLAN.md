# PLAN — этап 6: проверка на реальном стенде

Легенда: `[ ]` не начато, `[~]` в работе, `[x]` сделано, `[!]` заблокировано.

## Действия

- [x] Проверить сборки Windows x86, x64 и ARM64 на живой машине (владелец,
      Windows 11 ARM в Parallels на MacBook Air M5).
- [~] Разобраться, почему на сборке Windows x64 ручной запуск `pkcs11-tool`
      даёт ошибку 5, тогда как тест-кит на той же сборке проходит целиком.
- [ ] Собрать дословную команду и вывод отказа с `-v`, определить класс ошибки:
      `CKR_GENERAL_ERROR` (`0x5`) или Win32 `ERROR_ACCESS_DENIED`.
- [ ] Проверить профиль `FAKE_RUTOKEN_ECP` на реальном ПО, работающем с
      Рутокен ECP.
- [ ] Расширить покрытие `pkcs11-tool` в тест-ките за пределы `-I` и `-T` —
      настолько, насколько это воспроизводит найденный дефект.

## Этап 7 — юнит-тесты на ГОСТ

Начинаем сразу после этапа 6 (решение владельца от 2026-08-12).

- [ ] Юнит-тесты на `BotanStreebog256`.
- [ ] Юнит-тесты на `BotanGOST2012KeyGenerator`.
- [ ] Юнит-тесты на `BotanGOST2012Signer`.

## Не делаем

- **Восстанавливать удалённые джобы CI — не нужно.** Решение владельца от
  2026-08-12: форк тупиковый, собирать конфигурации, которые мы не поставляем,
  незачем. Записано в «Принятых решениях».
- **Приводить в порядок апстримные документы — не нужно.**
- **Трогать старые коммиты — нельзя** (§5, история append-only; отдельно
  подтверждено владельцем 2026-08-12). 21 коммит от 1–11 августа остаётся как
  есть; конвенция соблюдается начиная с `2ff7bd0`.

## Этап 5 — закрыт

- [x] Скопировать канонический `AGENTS.md` в корень репозитория без изменений.
- [x] Дописать приложение по проекту: команды, карта кода, принятые решения,
      отступления, версии, релизы, что проверяет владелец.
- [x] Завести шесть проектных документов.
- [x] Записать в `JOURNAL` решения владельца.
