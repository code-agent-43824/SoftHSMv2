# PLAN — этап 5: приведение репозитория к правилам

Легенда: `[ ]` не начато, `[~]` в работе, `[x]` сделано, `[!]` заблокировано.

## Действия

- [x] Скопировать канонический `AGENTS.md` в корень репозитория без изменений.
- [x] Дописать к нему приложение по проекту: команды, карта кода, принятые
      решения, отступления, версии, релизы, что проверяет владелец.
- [x] Завести `docs/ROADMAP.md`, `docs/PLAN.md`, `docs/STATUS.md`,
      `docs/WORKLOG.md`, `docs/JOURNAL.md` и `HANDOFF.md`.
- [x] Записать в `JOURNAL` решения владельца, санкционировавшие удаление
      матрицы CI, профиль `FAKE_RUTOKEN_ECP`, игнорирование `SOFTHSM2_CONF`,
      применение Botan и отказ от апстримных документов.
- [ ] Подтвердить у владельца этапы 6 и 7 дорожной карты.

## Не делаем на этом этапе

- **Восстанавливать удалённые джобы CI — не нужно.** Решение владельца от
  2026-08-12: форк тупиковый, собирать конфигурации, которые мы не поставляем,
  незачем. Записано в «Принятых решениях».
- **Приводить в порядок апстримные документы — не нужно.** `README.md`, `NEWS`,
  `*-NOTES.md` заброшены намеренно.
- **Переписывать историю ради формата коммитов — нельзя** (§5, история
  append-only). 21 коммит от 1–11 августа остаётся как есть; конвенция
  соблюдается начиная с `2ff7bd0`.

## Перенесено в следующие этапы

- Юнит-тесты на `BotanGOST2012Signer`, `BotanGOST2012KeyGenerator`,
  `BotanStreebog256` — этап 7. Владелец счёл полезным, но не сейчас
  (решение от 2026-08-12).
- Проверка профиля `FAKE_RUTOKEN_ECP` на реальном ПО под Рутокен ECP — этап 6.
