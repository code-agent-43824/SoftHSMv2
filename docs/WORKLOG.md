# WORKLOG — что делали

Новые записи сверху. Одна запись на кусок работы: план → сделано → дальше.

## 2026-09-10 — README test-kit признан проектной документацией

**План.** Исправить первопричину ошибочной публикации
`v2.7.0-portable.40`: release-workflow не относил
`tests/portable/README.md` к проектной документации, хотя именно так этот файл
используется и описан в правилах выпуска.

**Сделано.** Точный путь добавлен в documentation-only регулярное выражение.
Рядом добавлена исполняемая проверка этого контракта, чтобы последующая правка
выражения не смогла снова незаметно вывести README test-kit из документации.

**Почему намерение и workflow в одном коммите.** Обычный отдельный docs-only
коммит с планом прошёл бы через уже известный дефект и создал бы ещё один
ошибочный релиз. Исправление транка поэтому не разделено искусственно.

**Дальше.** Дождаться `CI` и полной матрицы на этом коммите, проверить новый
корректный релиз, затем отдельным docs-only коммитом обновить `STATUS` и
доказать, что публикация действительно пропускается.

## 2026-09-09 — три ошибки документации после сверки

**План.** По просьбе владельца исправить только подтверждённые ошибки в
документации, не затрагивая код, CI и настройки репозитория.

**Сделано.** Закрытый родительский пункт CTR-ACPKM в `PLAN` отмечен `[x]`;
в `STATUS` к классам ГОСТ 2012 без юнит-тестов добавлен `BotanStreebog512`;
финальный абзац `tests/portable/README.md` приведён к фактическому поведению
launcher: оба модуля загружаются на месте, evidence остаётся в test-kit, а
bundled-модуль использует каноническое пользовательское хранилище, не
одноразовую копию.

**Проверено.** Три места повторно сопоставлены с `run-test-kit.sh` и
`run-fresh-integration.sh`; противоречащих формулировок больше нет. Код и
workflow не менялись. Следующий этап проекта остаётся прежним — юнит-тесты на
ГОСТ 2012.

## 2026-09-09 — `docs/HANDOFF-VKO.md` убран

**План.** Правило владельца от 09.09: задача сделана — документ передачи
убираем, с отметкой в `JOURNAL`. Первый и единственный такой файл —
`docs/HANDOFF-VKO.md`, замеченный вчерашней сверкой документации.

**Сделано.** Файл удалён. Перед этим сверено, что из него ничего не пропадает:
обе ловушки Botan (подмена кривой TC26 paramSet A и порядок байт UKM) уже
описаны в `JOURNAL` за 21.08; запрет заменять группу на
`Botan::EC_Group(encodedOID)` был только там и перенесён в `JOURNAL`; стенд для
снятия новой трассы и граница по зашифрованию перенесены в `docs/PLAN.md`
отдельным **незакрытым** пунктом — это единственное, что в файле оставалось
незавершённым, а незавершённое не стирается (§2).

Живые ссылки на файл в `PLAN` и `RUTOKEN-EXTENSIONS` перенаправлены на
`JOURNAL`. Ссылки в записях `WORKLOG` за 20.08 и 21.08 оставлены: это запись о
том, что тогда было сделано, а не указатель.

**Итог.** В `docs/` остались ровно шесть канонических документов плюс
`HANDOFF.md` в корне, как и предписывает §2.

## 2026-09-09 — сверка документации с состоянием проекта

**План.** Владелец попросил проверить, что документация валидна и описывает
всё, что появилось: новые фичи, новые флаги.

**Как сверял.** Механически, а не чтением: перечислил из кода конфигурационные
ключи, параметры сборки `ENABLE_*`, сценарии сквозного клиента, переменные
окружения, которые он читает, и число механизмов профиля — и каждое искал в
документах.

**Дефектов в коде не нашлось.** Три ложные тревоги были моими же: шаблоны
grep без цифр в классе символов «потеряли» `DISABLE_OTHER_28147_MODES` и
`gost28147-modes`, а счёт механизмов дал 63 вместо 70, потому что не всякая
строка таблицы кончается именем механизма. Отдельно померил через PKCS #11:
70 под профилем, 105 без него — заявленное в `README.txt` число верно. Первый
пробник показал 0 и тоже был неправ: я печатал счётчик и вызывал функцию в
одном `printf`, где порядок вычисления аргументов не определён.

**Что было не так в документах — десять мест, все исправлены:**

- `AGENTS.md`, приложение по проекту: список флагов ГОСТ-2012 без двух
  512-битных; карта кода без `BotanStreebog512`, `BotanGOST2012KEG`, всего
  симметричного набора и `src/bin/export`; `ci.yml` описан как «одна джоба:
  собрать и проверить архив», хотя там уже восемь проверок; «текущий тег
  `v2.7.0-portable.29`» при фактическом `.38`; два новых ключа конфигурации не
  упомянуты вовсе.
- `docs/STATUS.md`: `softhsm2-export` описан как RSA/ECDSA/ГОСТ-256, хотя
  `--type gost512` есть с 05.09. И **внутреннее противоречие**: пункт про
  молчаливый шаблон утверждал «для всех ключей и в обоих профилях», что
  перестало быть правдой 08.09, когда появился `RUTOKEN_FORCE_SENSITIVE`.
- `docs/ROADMAP.md`: третье свойство продукта — «Стрибог-256 и
  ГОСТ Р 34.10-2012/256», хотя давно обе длины плюс симметричный набор и VKO.
- `tests/portable/README.md`: ни слова про `first-run`, `multi-token`,
  `gost28147-modes`, 512-битную половину и известные ответы CTR-ACPKM.
- Подсказка самого клиента: шесть читаемых переменных окружения в ней не
  назывались, включая новую `P11_28147_EXPECT_RESTRICTED`.

**Что проверял и что оказалось верным:** три ключа конфигурации описаны в
`README.txt`, `STATUS.md` и `RUTOKEN-EXTENSIONS.md`; все девять сценариев
клиента перечислены в его подсказке; ни в одном документе не осталось
утверждений «не реализовано» про то, что уже работает.

**Чем проверено запуском.** Клиент собран с `-Werror` и прогнан по шести
сценариям — все зелёные, каждый со своим кодом возврата. Полнота подсказки
проверена сравнением списка читаемых переменных со списком печатаемых:
расхождений не осталось.

## 2026-09-09 — `DISABLE_OTHER_28147_MODES`

**План.** Третий коммит: принимать только те два набора параметров, что
принимает устройство.

**Сделано.** Новый параметр конфигурации, умолчания по образцу
`RUTOKEN_FORCE_SENSITIVE`: под профилем включён, без профиля выключен. Пока
включён, ключ `CKK_GOST28147` с набором, отличным от CryptoPro-A и TC26-Z,
не создаётся — отказ `CKR_ATTRIBUTE_VALUE_INVALID` приходит на
`C_CreateObject` и на `C_GenerateKey`, то есть там же и с тем же кодом, что
у устройства, а не позже на `C_EncryptInit`.

**Одна граница проведена по решению, и вот она.** Отсутствующий или пустой
`CKA_GOST28147_PARAMS` пропускается: он означает CryptoPro-A, который
устройство принимает, а отказ ломал бы шаблон, просто не называющий атрибут.
Отвергается только явно заданное значение, которое не A и не Z.

**Чем проверено запуском.** Один сценарий в трёх конфигурациях, каждая своим
прогоном: профиль с умолчанием — CryptoPro-B отвергается `0x13`, A и Z
работают; профиль с `DISABLE_OTHER_28147_MODES = false` — B создаётся и
шифрует; без профиля — то же самое, обычный режим не изменился. Отдельно:
набор, которого шифр не знает, отвергается в обоих состояниях — при
ограничении на `C_CreateObject`, без него на `C_EncryptInit`, — и молча
CryptoPro-A не подменяется.

**Проверено отрицательно двумя сломами:** параметр, выключенный под профилем,
и снятая проверка при создании объекта — оба гейт ловит.

**Дальше.** Этап 7 — юнит-тесты на ГОСТ 2012.

## 2026-09-09 — ГОСТ 28147-89: вставка ключа для TC26-Z

**План.** Второй коммит: применять вставку ключа и к набору TC26-Z.

**Сделано.** `usesKeyMeshing()` исключает теперь только тестовый набор.
Граница (1024 байта) и константа не менялись — они уже сверены с устройством
на CryptoPro-A.

**Чем проверено запуском.** 4096 нулевых байт, ключ `00..1f`, синхропосылка
`5152535455565758`: байты 0..7 `13e9883fee6baa43` совпадали и раньше, байты
1024..1031 были `77bf085a2366484e` против `2a275c50d24ea662` у устройства, а
стали `2a275c50d24ea662`. CryptoPro-A не сдвинулся:
`4991e95e0439e602` / `9d0f7c83c25419f1`. Обе восьмёрки закреплены в гейте на
**приватном** ключе.

**Гейт проверен отрицательно:** возврат исключения TC26-Z роняет его с
названием ровно этого расхождения.

**Тестовый набор не тронут.** Устройство отказывается создавать с ним ключ,
данных нет, менять поведение не на чем.

**Дальше.** Коммит 3 — `DISABLE_OTHER_28147_MODES`.

## 2026-09-09 — ГОСТ 28147-89: набор параметров у приватного ключа

**План.** Первый из трёх коммитов: снять блокер.

**Сделано.** `getSymmetricKey` читает `CKA_GOST28147_PARAMS` так же, как
читает значение ключа: у приватного объекта — через `token->decrypt`. Раньше
шифру доставался шифротекст вместо OID, `setParamSet` его не опознавал, а
вызывающий превращал отказ в `CKR_MECHANISM_INVALID`.

**Чем проверено запуском.** До правки на собранном модуле: ключ
`CKA_PRIVATE = FALSE` шифрует и совпадает с устройством, тот же ключ с
`CKA_PRIVATE = TRUE` даёт `0x70`. После правки приватный ключ выдаёт **тот же
шифротекст**, что публичный, и совпадает с присланными значениями с
устройства: все 64 байта на CryptoPro-A, и обе снятые восьмёрки на 4096 байтах
(`4991e95e0439e602` в начале, `9d0f7c83c25419f1` на границе 1024). Закреплены
все четыре пути, идущие через `getSymmetricKey`: шифрование, расшифрование,
MAC и обёртывание — все с приватным ключом.

**Гейт проверен отрицательно:** возврат к сырому чтению роняет его с
`expected 0x0, got 0x70`.

**Соседние сырые чтения — искал, не нашёл.** Все прочие места, читающие
атрибуты возможно-приватного объекта (`getRSAPrivateKey`, `getDSA*`, `getEC*`,
`getED*`, `getDH*`, `getGOST*`, `UnwrapMechRsaAesKw`, пути производных
и обёртываемых ключей), ветвятся по `CKA_PRIVATE` и расшифровывают. Набор
параметров в `getSymmetricKey` был единственным исключением.

**Дальше.** Коммит 2 — вставка ключа для TC26-Z.

## 2026-09-09 — ГОСТ 28147-89: намерение

**План.** Задание владельца: шифр реализован и считает верно, но недоступен
приложениям из-за одной строки, и в одном месте расходится с устройством. Три
коммита; первые два самостоятельны.

**Всё воспроизведено на собранном модуле до начала работы**, чтобы не чинить
по описанию:

| ключ | `C_EncryptInit(CKM_GOST28147)` |
| --- | --- |
| `CKA_PRIVATE = FALSE` | `CKR_OK` |
| `CKA_PRIVATE = TRUE` | `0x70 CKR_MECHANISM_INVALID` |

На публичном ключе шифротекст совпал с присланными владельцем значениями с
устройства: 64 байта на CryptoPro-A — побайтно все 64; 4096 байт — байты 0..7
`4991e95e0439e602` и 1024..1031 `9d0f7c83c25419f1`. У TC26-Z первые восемь
байт `13e9883fee6baa43` совпали, а на границе 1024 у нас `77bf085a2366484e`
против `2a275c50d24ea662` у устройства — то есть шифр и S-блоки верны, не
хватает вставки ключа. CryptoPro-B сейчас принимается.

**Дальше.** Коммит 1 — чтение набора параметров у приватного ключа.

## 2026-09-08 — `RUTOKEN_FORCE_SENSITIVE`

**План.** Третий коммит задания: под профилем закрытый ключ не должен отдавать
своё содержимое обычным `C_GetAttributeValue`, как не отдаёт настоящий Рутокен.

**Сделано.** Новый параметр конфигурации. По умолчанию равен состоянию
`FAKE_RUTOKEN_ECP`: под профилем включён, без него выключен — то есть обычный
режим SoftHSM не меняется вовсе, пока параметр не выписан в конфигурации
явно. Подстановка сделана в двух точках, `C_GenerateKey` и
`C_GenerateKeyPair` (у пары — только шаблон закрытого ключа, у открытого таких
атрибутов нет): шаблон копируется, и в него **дописывается только то, чего в
нём не было**. Дальше всё идёт обычным путём, поэтому
`CKA_ALWAYS_SENSITIVE` и `CKA_NEVER_EXTRACTABLE` выставляются согласованно
сами, из итоговых значений.

**Правило пер-атрибутное, и это видно в тесте.** Шаблон, назвавший только
`CKA_SENSITIVE = FALSE`, получает это значение — но `CKA_EXTRACTABLE`
подставляется `FALSE`, и ключ всё равно не читается. Так сформулировано
задание («подставляет недостающее, а не переопределяет заданное», «правило
одинаково для обоих атрибутов»), и обе односторонние комбинации закреплены
тестом именно затем, чтобы этот эффект был виден, а не обнаружился потом.

**Что пришлось поправить в тестах, а не в коде.** `generateGOSTSecret`
проверял размер выработанного ключа чтением `CKA_VALUE` — под профилем это
теперь законный отказ. Хелпер стал просить читаемый ключ явно
(`CKA_SENSITIVE = FALSE`, `CKA_EXTRACTABLE = TRUE`); заодно это и есть
проверка правила «заданное сильнее». Пробовал вместо этого `CKA_VALUE_LEN` —
у этих ключей его нет, `CKR_ATTRIBUTE_TYPE_INVALID`.

**Чем проверено запуском.** Сценарий в профильном гейте: молчаливый шаблон
у `C_GenerateKey` и у `C_GenerateKeyPair` — `CKR_ATTRIBUTE_SENSITIVE` и
согласованные четыре флага; явная просьба о читаемом ключе — генерация
проходит и значение читается; обе односторонние комбинации; открытая половина
пары читается; импорт через `C_CreateObject` отдаёт заведённое значение.
Поведение без профиля закреплено прежним `verifySilentTemplateKeyIsReadable`
в гейте `core-behaviour` и не изменилось.

**Проверено отрицательно двумя сломами:** параметр, выключенный под профилем,
и подстановка, переписывающая заданное вместо недостающего — оба гейт ловит.

**Дальше.** Этап 7 — юнит-тесты на ГОСТ 2012.

## 2026-09-08 — красный транк после раскладки токенов, починен вперёд

**Что было.** Коммит `c59c207` уронил `CI`: гейт профиля утверждал «токен
только в слоте 0», а запасной неинициализированный токен теперь виден на
следующем слоте намеренно. В сообщении того коммита написано, что все три
гейта проходят, — **это неверно**, второй гейт падал уже локально, и я принял
за проверку печать «GATE 2 ok», не связанную с кодом возврата. Разбор в
`docs/JOURNAL.md`.

**Починено вперёд.** Утверждение приведено к новому контракту: занятые слоты
идут подряд с нуля, инициализированные первыми, запасной последним и
неинициализированным; проверка пустого слота смотрит на первый слот за
занятыми, а не на слот 1 жёстко.

**Чем проверено.** Восемь прогонов — обе команды утилиты на холодной машине,
`first-run`, `multi-token` и три гейта — каждый со своим кодом возврата,
проверяемым отдельно, а не печатью следом за командой.

## 2026-09-08 — токены разложены по слотам фасада

**План.** Второй коммит задания: показать каждый токен на своём слоте вместо
одного случайно выбранного на нулевом.

**Сделано.** Раскладка считается в одном месте — `fakeRutokenLayout()`:
инициализированные токены по возрастанию времени создания, затем запасной
неинициализированный, не длиннее пятнадцати слотов профиля. Все точки входа,
адресуемые слотом, теперь спрашивают у неё: `C_GetSlotList`, `C_GetSlotInfo`,
`C_GetTokenInfo`, `C_GetMechanismList`, `C_GetMechanismInfo`, `C_InitToken`,
`C_OpenSession`, `C_CloseAllSessions`, `C_EX_GetTokenInfoExtended`. Обратное
отображение понадобилось там, где слот приходит из сессии:
`C_GetSessionInfo` возвращал ноль всегда, `C_EX_GetTokenName` спрашивал про
нулевой слот всегда — оба теперь называют тот токен, на котором сессия открыта.

**Время создания.** Новый вендорский атрибут объекта токена
`CKA_OS_TOKENCREATED`, пишется один раз при создании и никогда не
переписывается: `resetToken` сохраняет объект, значит переинициализация токена
сохраняет и его место. Реализован в обоих бэкендах, файловом и БД.

**Отступление от текста задания, названное вслух.** В задании предложено
хранить секунды, а совпадение разрешать серийником. Так полный порядок
получается, но заявленная цель — «вновь созданный токен встаёт в конец и не
сдвигает те, что уже разложены» — при этом ломается: два токена, созданные
одним скриптом, попадают в одну секунду, и порядок проваливается на серийник,
который производный от случайного UUID. Это не гипотеза: первый вариант с
секундами именно так и провалил собственный тест — «layout two» встал перед
«layout one». Поэтому храню **микросекунды**, восемь байт big-endian; серийник
остался последним средством. Формат байтовый, а не `CK_ULONG`, потому что на
32-битной Windows `CK_ULONG` микросекунды не вмещает.

**Попутно найдено и починено.** Профильный `C_GetSlotList` не проходил через
`SlotManager::getSlotList` и потому не пополнял запасной токен: внутри одного
процесса после `--init-token --free` нового запасного не появлялось до
перезагрузки библиотеки. Теперь профиль запрашивает у `SlotManager` размер и
выбрасывает его — правило пополнения осталось в одном месте.

**Чем проверено запуском.** Сценарий `multi-token` в гейте: три токена
заводятся подряд через `C_InitToken` на запасном слоте, после каждого
проверяется, что новый встал следующим и ни один прежний не сдвинулся; затем
библиотека грузится заново и раскладка совпадает; затем для каждого слота
сверяются `C_GetTokenInfo`, число механизмов, `C_EX_GetTokenInfoExtended`,
`C_GetSessionInfo` и `C_EX_GetTokenName` — каждый про свой токен. Пустые
слоты дают `CKR_TOKEN_NOT_PRESENT`, слот 15 — `CKR_SLOT_ID_INVALID`. Отдельно
руками: `softhsm2-util --init-token --free` под профилем теперь работает, чего
раньше не было вовсе.

**Проверено отрицательно двумя сломами:** адресация нулевого токена за любым
слотом и порядок без учёта времени создания — оба гейт ловит.

**Известная цена, как и обещано.** Удаление токена из середины сдвигает те,
что за ним: позицию держит только порядок, а не сохранённое отображение
«токен → слот». Приложения, перечисляющие слоты, это не ломает; прибитые к
слоту 0 — ломает.

**Инференция, а не измерение.** Слот с токеном теперь отдаёт описание
`Aktiv Rutoken ECP <n>`. У эталонного устройства считан один считыватель, с
номером 0; нумерация остальных выведена из него. Оставить занятый слот вовсе
без описания было бы явно неверно, поэтому заполняем — но это догадка, и здесь
она названа.

**Дальше.** Коммит 3 — `RUTOKEN_FORCE_SENSITIVE`.

## 2026-09-08 — каталог токенов создаётся сам

**План.** Первый из трёх коммитов задания, самый мелкий: выполнить обещание
`README.txt` про `directories.tokendir`.

**Что оказалось при проверке — не то, что было в задании.** Модуль каталог
создаёт и создавал: `ObjectStore` делает его при открытии. Ломались
**утилиты** — `softhsm2-util` и `softhsm2-export` открывают каталог напрямую
через `Directory`, мимо `ObjectStore`, и на машине, где модуль ни разу не
загружали, падали с «Failed to enumerate object store». Проверено на
опубликованном `v2.7.0-portable.36`: загрузка модуля в пустом `HOME` каталог
создаёт, а `softhsm2-util --show-slots` в таком же пустом `HOME` — падает.

**Почему дефект дожил до сегодня — две причины, и обе снимаются.** Ручной
`mkdir` в `ci.yml` перед единственным шагом, где утилиты встречают чистую
машину. И порядок в тест-ките: сценарий сперва грузит модуль, тот делает
каталог, и утилита его отсутствия уже не видит.

**Сделано.** Каталог создаётся при загрузке конфигурации, в
`SimpleConfigLoader`, — то есть на общем пути и модуля, и обеих утилит. Только
в portable-режиме (`SOFTHSM2_PORTABLE_USER_CONFIG`): в обычной сборке каталог
задаёт администратор, и молча создавать опечатку в пути значило бы прятать
ошибку, а не чинить её. Ручной `mkdir` из `ci.yml` убран — теперь этот шаг
и есть проверка.

**Чем проверено запуском.** В пустом `HOME`: до правки `--show-slots` и
`--init-token --free` падают обе (проверено и на опубликованном модуле, и с
явным `--module`, чтобы исключить поиск библиотеки), после правки обе проходят
и каталог появляется. Сквозной сценарий `first-run` добавлен в гейт и на
шести платформах закрепляет половину модуля.

**Названо честно.** Сценарий `first-run` — **не** сторож этого дефекта: он
проверяет половину модуля, которая была верна и до правки. Сторож утилит —
шаг `ci.yml` без `mkdir`.

**Дальше.** Коммит 2 — раскладка токенов по слотам.

## 2026-09-08 — профиль: слоты, sensitive, каталог токенов — намерение

**План.** Задание владельца: три доработки профиля `FAKE_RUTOKEN_ECP`, все про
то, чтобы модуль вёл себя как устройство там, где сейчас ведёт себя как
программная заглушка. Три коммита, порядок и объём — в `docs/PLAN.md`.

**Что проверено до начала работы.** Дефект каталога воспроизведён на
опубликованном модуле `v2.7.0-portable.36`: в пустом `HOME` появляется
`softhsm/softhsm.conf`, но не `softhsm/tokens`, и `softhsm2-util --show-slots`
печатает «Failed to enumerate object store». То есть обещание `README.txt`
расходится с поведением, и ручной `mkdir` в `ci.yml` ровно это и скрывает.

**Дальше.** Коммит 1 — каталог токенов.

## 2026-09-07 — Кузнечик: линейное преобразование на таблицах

**План.** Второй коммит задания: Кузнечик работал на порядок медленнее Магмы,
хотя на устройстве он быстрее. Приёмка — результат не меняется ни на байт.

**Причина.** Линейное преобразование считалось честно по определению: шестнадцать
раундов, в каждом скалярное произведение в GF(2⁸) по шестнадцати байтам. 256
умножений в поле на каждый блок каждого раунда, 2304 на блок. Это же
преобразование стоит в развёртке ключа, а при секции в 64 байта развёртка идёт
каждые четыре блока — потому вариант со сменой ключа проседал ещё втрое.

**Сделано.** L линейно над GF(2)¹²⁸, значит L(x) — сумма его действий на каждый
байт по отдельности, и это укладывается в таблицу. Три таблицы: L∘S для
шифрования и развёртки, L⁻¹ для расшифрования, и 32 раундовые константы, которые
от ключа не зависят и считались заново на каждой развёртке. Каждая клетка — блок
из двух `uint64_t`, так что шестнадцать клеток складываются 32 операциями XOR
вместо 256. Таблицы **строятся вызовом тех же функций**, что и раньше считали в
лоб, а не переписаны руками: разойтись с определением они не могут. Живут в
function-local static, инициализация которого в C++11 потокобезопасна — попутно
ушла прежняя ленивая инициализация обратной подстановки, которая потокобезопасной
не была.

**Замеры, 1 МиБ через PKCS #11, лучшее из пяти:**

| | было | стало | устройство |
| --- | --- | --- | --- |
| Кузнечик, без смены ключа | 1,12 МиБ/с | **86,0 МиБ/с** | 31,7 МиБ/с |
| Кузнечик, секция 512 бит | 0,36 МиБ/с | **42,4 МиБ/с** | — |
| Магма, без смены ключа | 31,4 МиБ/с | 32,8 МиБ/с | 26,9 МиБ/с |
| Магма, секция 512 бит | 22,1 МиБ/с | 22,4 МиБ/с | — |

Кузнечик стал быстрее Магмы — тот же порядок, что у устройства, где он тоже
быстрее. Магма не менялась, её строки здесь как контроль шума измерения.

**Приёмка «ни на байт».** Отпечаток из 19 456 байт на каждый шифр — развёртка
ключа, `encryptBlock`, `decryptBlock`, CTR, OMAC, MGM в обе стороны, на 64
разных ключах — совпал побайтно до и после. Гейт со всеми векторами RFC и обоими
контрольными примерами устройства зелёный. Отрицательная проверка: один
перевёрнутый бит в одной клетке таблицы роняет и гейт, и отпечаток.

**Дальше.** Этап 7 — юнит-тесты на ГОСТ 2012.

## 2026-09-07 — CTR-ACPKM: поле периода читается как биты

**План.** Первый из двух коммитов задания: единицы поля периода.

**Сделано.** Длина секции теперь `поле / 8`, а проверка допустимости требует
кратности размеру блока **в битах** — 64 у Магмы, 128 у Кузнечика. Значение 0
«смены ключа нет» сохранено. Больше в CTR-ACPKM не изменилось ничего:
константы выработки ACPKM, непрерывность счётчика и гамма были верны и до
правки, что и подтвердил прогон.

**Чем проверено запуском.** Оба контрольных примера с устройства закреплены в
гейте: ключ импортируется `C_CreateObject` с известным `CKA_VALUE`, шифруются
16 384 нулевых байта с полем 512, сверяются первые 128 байт с эталоном и
SHA-256 всех 16 384 байт (`810196…bda3` у Магмы, `6004af…f34d` у Кузнечика) —
это те же значения, что владелец снял с устройства. Совпало у обоих шифров.
Отдельно закреплено, что поле 4096 (та же секция, записанная по-старому,
512 байт) даёт тот же первый 64-байтовый отрезок и расходится ровно с 64-го
байта — то самое место расхождения, что записано в слепке. Недопустимые
периоды (1, полблока, блок ± 1, полтора блока и старое значение 16) отвергают
и `C_EncryptInit`, и `C_DecryptInit`; допустимые (0, блок, четыре блока, 512)
проходят полный круг.

**Гейт проверен отрицательно двумя сломами:** возврат к чтению поля как байтов
роняет известный ответ и печатает ровно старый шифротекст; возврат проверки
допустимости к байтам роняет отказ по периоду 32.

**Код возврата не менялся.** Недопустимый период отвергается как
`CKR_MECHANISM_INVALID`, а не `CKR_MECHANISM_PARAM_INVALID`: так этот модуль
сообщает о любом отказе инициализации шифра, и это отображение старше правила
о периоде. Закреплено как есть — менять его значит менять поведение всех
симметричных механизмов сразу, а это отдельное решение.

**Обновлено попутно.** Сценарий гейта использовал поле `0x10`; теперь
`0x80` — один блок Кузнечика, так что 33-байтовая полезная нагрузка проходит
через две смены ключа. В `packaging/portable/README.txt` появилось описание
параметра с явным предупреждением о несовместимости со старыми выпусками.

**Дальше.** Коммит 2 — скорость Кузнечика.

## 2026-09-07 — CTR-ACPKM: единицы периода и скорость Кузнечика, намерение

**План.** Задание владельца: две правки в CTR-ACPKM — ломающая совместимость
ошибка в единицах поля периода и потеря скорости у Кузнечика. К заданию
приложен слепок с реального устройства (Рутокен ECP, прошивка 26.2, серийный
номер 3cbc56f9, штатная `rtPKCS11ECP.dll` 2.19). Два коммита, сначала
корректность, потом скорость, чтобы изменение шифротекста и проверка «результат
не изменился» не смешались в одном диффе.

**Что проверено до начала работы, чтобы не переоткрывать.** Слепок сверен с
текстом задания: длины, хеши и первые 128 байт совпадают. Обе трактовки поля
прогнаны **нашими же** шифрами на 16 КБ нулей: при длине секции = поле / 8
шифротекст совпадает с устройством побайтно на всех 16 384 байтах у обоих
шифров (sha256 `810196…bda3` для Магмы и `6004af…f34d` для Кузнечика); при
длине секции = полю расхождение начинается ровно с 65-го байта, как и записано
в слепке. Значит ошибка действительно только в единицах: константы выработки
ACPKM, непрерывность счётчика и сама гамма уже верны, и трогать их не нужно.

**Заранее известная цена.** Шифротекст при тех же параметрах изменится — это и
есть цель. Собственный сценарий гейта использует поле `0x00000010`; под новой
трактовкой это 16 бит, меньше блока Кузнечика, и станет недопустимым — значение
надо обновить. Смена ключа станет происходить в восемь раз чаще, шифрование
слегка замедлится: правильное поведение, а не регресс. Хранилище объектов
правка не затрагивает, миграция не нужна.

**Дальше.** Коммит 1 — единицы поля периода.

## 2026-09-06 — политика параметра набора сведена в одно правило

**План.** Задание владельца: `C_SignInit` и `C_VerifyInit` не должны отвергать
OID набора параметров у совместных механизмов подписи ГОСТ-2012, правило
написать один раз и применить единообразно к обеим длинам, к хешу, подписи и
проверке, в обоих состояниях профиля.

**Что оказалось при проверке.** Само поведение подписи и проверки уже было
исправлено 05.09 вместе с 512-битной работой — и подпись, и проверка уже
принимали свой OID и отвергали чужой. Незакрытым оставалось второе требование
задания, и оно же было причиной исходного дефекта: **правило существовало
шестью копиями**. `C_DigestInit`, `C_SignInit` и `C_VerifyInit` проверяли
параметр каждый у себя, по разу на длину, и каждая копия сама называла
ожидаемый OID. Именно так копия в подписи и разошлась с копией в хешировании.

**Сделано.** Одна функция `gostMechanismParameterAccepted()` выводит ожидаемый
набор из самого механизма; все десять точек — шесть с OID и четыре
«параметра быть не должно» — зовут её. Поведение не изменилось ни в одной
клетке таблицы, изменилось число мест, где его можно забыть. Ветка по
умолчанию требует отсутствия параметра, так что новый механизм ГОСТ-2012 не
проскочит мимо политики молча.

**Чем проверено.** Табличный тест в гейте: шесть случаев (нет параметра, свой
набор, набор соседней длины, обрезанный OID, удлинённый OID, та же длина с
другими байтами) × три точки входа × две длины, плюс отказ у механизмов,
подписывающих готовую свёртку — 40 утверждений. Отдельной программой прогнаны
38 клеток на собранном модуле с профилем включённым и выключенным: вывод
побайтово одинаков в двух состояниях профиля, расхождений нет. Гейт проверен
отрицательно двумя разными сломами: «принимать что угодно» и «у 512-битных
механизмов ожидать 256-битный OID»; оба он ловит.

**Что не менялось.** Криптография, форматы, список механизмов профиля,
ГОСТ 28147-89.

**Дальше.** Этап 7 — юнит-тесты на ГОСТ 2012.

## 2026-09-05 — ГОСТ Р 34.10-2012/512 завершён

**План.** Доделать оставшиеся четыре коммита: подпись и проверка, экспорт,
сквозной сценарий, документы.

**Сделано.** Подпись и проверка для обеих длин. Подписант перестал быть
256-битным: размер дайджеста, скаляра, подписи и выбор Стрибога следуют из
механизма, а кривая сверяется с этим размером. **Проверка написана с нуля** —
у `BotanGOST2012Signer` не было ни класса открытого ключа, ни `verify*`, и
`C_VerifyInit` не имел ветки для ГОСТ-2012 вовсе; заодно это закрыло проверку
для 256 бит. PKCS #8-экспорт: `--type gost512`, OID-ы выбираются по размеру
скаляра, результат разобран системным OpenSSL. Сквозной 512-битный сценарий
добавлен в профильный гейт, то есть идёт на каждый пуш и на шести платформах
при релизе.

**Чем проверено.** Обе длины: подпись, `C_Verify` на хорошей подписи `0x0`; на
подписи с перевёрнутым битом и на другом сообщении — `0xc0`; 512-битный
механизм с 256-битным набором параметров отвергается; paramSetB отвергается.
Оба профиля, `FAKE_RUTOKEN_ECP` true и false. Экспорт: OpenSSL печатает
`GOST R 34.10-2012 with 512 bit modulus`, `ParamSet A`,
`GOST R 34.11-2012 with 512 bit hash` и 64-байтовый OCTET STRING. Гейт проверен
отрицательно: `verifyFinal`, возвращающий успех всегда, роняет его.

**Не выполнено.** Приёмочное приложение `hardware-encryption-test` в природе
не найдено — ни в репозитории, ни среди присланных файлов, ни в истории. Шесть
пунктов приёмки не прогонялись.

**Дальше.** Этап 7 — юнит-тесты на ГОСТ 2012; список классов без покрытия
вырос на `BotanStreebog512` и обе стороны подписанта.

## 2026-09-05 — ГОСТ Р 34.10-2012/512: параметр, флаги, хеш, генерация

**План.** Первые четыре коммита из восьми, по порядку из `docs/PLAN.md`.

**Сделано и проверено запуском на portable-модуле:**

1. **Параметр механизма подписи.** `C_SignInit` для
   `CKM_GOSTR3410_WITH_GOSTR3411_2012_256` теперь принимает DER-OID набора
   параметров, как это делает `C_DigestInit`. Замерено до правки: без
   параметра `0x0` и подпись 64 байта, с набором — `0x71`. После правки —
   `0x0` в обоих случаях, а чужой набор (512-битный) и обрезанный OID
   по-прежнему `0x71`. Чинит подпись ГОСТ-256 для ПО под Рутокен, до всякого
   512-битного кода.
2. **Флаги** `ENABLE_GOST_3410_2012_512` и `ENABLE_GOST_3411_2012_512`, во всех
   трёх portable-скриптах. Первый включает второй: 512-битная совместная
   подпись хеширует Стрибогом-512.
3. **Стрибог-512.** Совпал с RFC 6986 §10.1.1 побайтово, one-shot и multipart.
   Вектор взят из самого RFC, а не по памяти, и порядок байт проверен
   перекрёстно: разворот печатного 256-битного результата RFC даёт значение,
   уже сверенное в репозитории.
4. **Генерация 512.** `CKM_GOSTR3410_512_KEY_PAIR_GEN` на кривой `gost_512A`:
   открытый ключ 128 байт little-endian X‖Y, скаляр 64 байта, тип ключа
   `CKK_GOSTR3410_512`. paramSetB/C отвергаются, а не подменяются на A.

**Три дефекта, найденные прогоном, а не чтением** — все в четвёртом пункте:
`P11GOST*KeyObj::init` переклеивал 512-битному ключу 256-битный тип, и
создание падало уже после генерации; `CKA_KEY_GEN_MECHANISM` записывался
256-битным всегда; вставка в таблицу механизмов разрезала `#ifdef` и уносила
`CKM_GOSTR3410_WITH_GOSTR3411_2012_256` под чужой флаг. Подробности в
`JOURNAL`.

**Дальше.** Коммит 5 — подпись и проверка. Проверки ГОСТ-2012 в форке нет
**вовсе**: у `BotanGOST2012Signer` нет ни класса открытого ключа, ни
`verify*`, а в `C_VerifyInit` нет ветки. Это больше, чем «ветки в
`C_SignInit` / `C_VerifyInit`» из задания. Затем экспорт, сквозной тест,
документы.

## 2026-09-05 — ГОСТ Р 34.10-2012/512 и Стрибог-512: намерение

**План.** Задание владельца: поддержать 512-битные ключи ГОСТ Р 34.10-2012 —
генерацию, подпись, проверку и Стрибог-512 — так, чтобы его приложение
работало с portable-модулем в профиле `FAKE_RUTOKEN_ECP` по всем 512-битным
сценариям. Разбить на самостоятельные коммиты, каждый собирается и проходит CI
сам по себе; порядок и объём — в `docs/PLAN.md`.

**Что уже проверено до начала работы, чтобы не переоткрывать:**

- Кривая `gost_512A` с нужным OID `1.2.643.7.1.2.1.2.1` есть в пиннутом
  Botan 2.19.5 (`src/lib/pubkey/ec_group/ec_named.cpp:127`) — сверено в
  распакованном дереве сборки, а не по памяти. paramSetB и paramSetC там
  отсутствуют.
- Стрибог-512 даёт уже включённый модуль `streebog`; расширять
  minimized-build не нужно.
- **Первая находка, ещё до кода.** `C_SignInit` для
  `CKM_GOSTR3410_WITH_GOSTR3411_2012_256` отвергает любой параметр
  (`SoftHSM.cpp`, ветка `WITH_GOST_3410_2012_256`), тогда как `C_DigestInit`
  такой же OID принимает. Значит подпись ГОСТ-256 через ПО под Рутокен, которое
  этот OID всегда шлёт, сломана прямо сейчас. Это первый коммит.
- **Вторая находка.** В `C_VerifyInit` ветки `WITH_GOST_3410_2012_256` нет
  вообще — проверка подписи ГОСТ-2012 не подключена, хотя ниже по коду
  (`#if defined(WITH_GOST) || defined(WITH_GOST_3410_2012_256)`) обвязка для
  неё есть. 512-битная проверка требует того же места, так что 256-битная
  ветка добавляется рядом; это добавление, а не переписывание.

**Заблокировано.** Приёмочное приложение `hardware-encryption-test` и его
шесть пунктов приёмки в задании названы, но в репозитории, в переданных
владельцем файлах и в истории отсутствуют. Прогнать его нечем — этот пункт
проверки останется невыполненным, и в отчёте он будет назван невыполненным, а
не пересказан. Остальные пять пунктов проверки выполняются.

**Дальше.** Коммит 1 — параметр механизма.

## 2026-08-31 — завершён симметричный набор ГОСТ/Rutoken

**План.** Завершить начатую реализацию: MGM для Кузнечика и Магмы,
`CKM_GOST28147_KEY_WRAP`, KExp15 для обоих новых шифров и сквозной PKCS #11
гейт. Не ограничиваться взаимным round-trip: математические результаты
сверить с опубликованными RFC-векторами, а повреждённые теги проверить
отрицательно.

**Сделано.** MGM реализован по RFC 9058 для блоков 64 и 128 бит с
`CK_MGM_PARAMS`/`CK_GCM_PARAMS`, AAD и тегами от 32 бит до размера блока.
Ограничения длины сообщений соблюдают строгие границы RFC для обоих блоков.
Добавлены KExp15 по RFC 9189 (`K_MAC || K_ENC`, OMAC над `UKM || key`,
CTR над `key || tag`) для Kuznechik/Magma twin-key и legacy-обёртка
ГОСТ 28147-89 с 32-битной имитовставкой. Последняя принимает явный 8-байтовый
MAC-IV или, как предписывает PKCS #11/Rutoken, отсутствие параметра с нулевым
IV; KExp15 принимает также `CKK_GENERIC_SECRET`. Все механизмы подключены к
`C_Encrypt`/`C_Decrypt` и `C_WrapKey`/`C_UnwrapKey`; ГОСТ 28147 ECB/CFB
заодно включены в общий симметричный маршрутизатор, где их раньше ошибочно
перехватывала асимметричная ветка.

Сквозной `rutoken-profile` теперь генерирует все три типа ключей через
`C_GenerateKey`, импортирует их через `C_CreateObject` и проверяет RFC 7801
Kuznechik ECB, RFC 8891 Magma ECB, оба RFC 9058 MGM-вектора, CTR-ACPKM,
Kuznechik/Magma/ГОСТ 28147 MAC с `C_Verify`, оба KExp15 (включая
`CKK_GENERIC_SECRET`) и ГОСТ 28147 ECB/CFB/key-wrap. Изменённые теги
MGM/KExp15 и имитовставка ГОСТ-wrap дают
`CKR_ENCRYPTED_DATA_INVALID`/`CKR_WRAPPED_KEY_INVALID`. Локальный профильный
прогон прошёл целиком. Он нашёл два соседних дефекта объектного слоя:
импорт TC26-ключа пытался вычислить только AES/DES KCV и возвращал
`CKR_GENERAL_ERROR`, а twin-key нельзя было импортировать без включённого
ГОСТ-2012. Оба ограничения сняты; при этом `CKA_VALUE_LEN` у производного
twin-key сохранён прежним исправлением.

**Дальше.** Запустить push-CI и полный автоматический portable workflow,
прочитать логи шести verifier jobs и проверить опубликованные архивы.

## 2026-08-30 — симметричная криптография ГОСТ/Rutoken

**План.** По просьбе владельца реализовать не только блочные примитивы, а весь
симметричный набор, который опубликован и рекламируется профилем Rutoken:
генерацию 256-битных ключей ГОСТ 28147-89, Кузнечик и Магма; ECB и
ГОСТ-28147 CFB; CTR-ACPKM; MAC; аутентифицированный MGM; обёртывание
ГОСТ-28147 и KExp15. ABI брать из официального `pkcs11tc26_18.h`, параметры и
ограничения — из документации Rutoken/TC26, результаты — из независимых
векторов стандартов и RFC. Реализация должна одинаково работать во всех шести
portable-сборках и не зависеть от наличия новых ГОСТ-алгоритмов в системном
OpenSSL/Botan.

Отдельно зафиксировано ограничение точности: механизм `0x8000000B` присутствует
в снятом списке эталонного устройства, но отсутствует в актуальном официальном
заголовке и не имеет опубликованной семантики. До аппаратной трассы он остаётся
только частью точного списка профиля — придумывать для него алгоритм нельзя.

**Сделано.** Уточнены точные числовые значения типов ключей и механизмов,
форматы CTR-ACPKM (`period || half-block IV`), MGM (`CK_GCM_PARAMS`) и KExp15,
а также текущие точки расширения SoftHSM: объекты ключей, генерация,
Encrypt/Decrypt, Sign/Verify и Wrap/Unwrap.

**Дальше.** Добавить общий переносимый криптографический слой, начать с
примитивов и KAT, затем подключить PKCS #11-маршрутизацию и сквозной test-kit.

## 2026-08-30 — принудительный экспорт ГОСТ-ключей

**План.** По просьбе владельца расширить `softhsm2-export`: помимо RSA и EC
выгружать закрытые ключи ГОСТ Р 34.10-2012/256 в PKCS #8 PEM/DER, включая
явно sensitive/non-extractable объекты. Стандартный PKCS #11 не менять.

**Сделано.** Добавлен `--type gost`, внутренний экспорт объекта
`CKK_GOSTR3410` и отсутствовавшее PKCS #8-кодирование лёгкой реализации
ГОСТ-2012. `PrivateKeyInfo` содержит OID `1.2.643.7.1.1.1.1`, параметры кривой
из объекта и 32-байтовый little-endian скаляр; для старых CryptoPro-кривых
добавляется обязательный OID Стрибог-256 по RFC 9215. Unix/Windows test-kit и
push-CI экспортируют persistent sensitive/non-extractable ГОСТ-ключ и независимо
проверяют ASN.1 и три OID через OpenSSL. Обычная и реальная Botan 2.19.5
конфигурации собраны локально; сквозной локальный GOST prepare/export прошёл,
DER имеет права `0600`.

Первый push-CI выявил старую несогласованность общего выбора слота: число
слотов запрашивалось с `tokenPresent=true`, а сам список — с `false`. Обычный
SoftHSM это скрывал, но профиль Rutoken честно вернул 15 слотов вместо одного
и `CKR_BUFFER_TOO_SMALL`. Оба прохода теперь используют один режим: поиск по
метке/серийному номеру смотрит только слоты с токенами, а `--free` — все.
Регрессия воспроизведена на включённом профиле; после исправления экспортёр
находит токен по метке и повторно выдаёт побайтно тот же ГОСТовый PKCS #8.

Push-CI `33321089801` прошёл на точном SHA
`45eca04dd7b6b808533189ce46c33922412f3557`. Его гейт инициализированного
Rutoken-профиля подтвердил поиск токена по метке `Rutoken ECP`, экспорт DER и
наличие OID ГОСТ-2012/256, CryptoPro-A и Стрибог-256. Автоматический portable
workflow `33321206509` успешно завершил 6/6 build jobs и 6/6 независимых
verifier jobs и опубликовал `v2.7.0-portable.30` на том же SHA. В логах каждого
verifier есть GOST и RSA import/export с повторной подписью и `[UTIL] PASS` для
автономного RSA/ECDSA/GOST PKCS #8 export. Все 12 ZIP и `SHA256SUMS` скачаны
заново: совпали 12/12 хешей, вложенных ZIP нет, `softhsm2-export` есть во всех
12 архивах, а ELF/PE/Mach-O архитектуры соответствуют именам пакетов.

**Дальше.** Вернуться к реальной трассе расшифрования Рутокен Плагина.

## 2026-08-24 — ID формата `pkcs11-tool` в `softhsm2-export`

**План.** После ручной проверки владелец попросил принимать в
`softhsm2-export --id` не только слитный hex (`6d79727361`), но и вывод
`pkcs11-tool` (`6d:79:72:73:61`). Изменить один парсер, добавить регрессионную
проверку в portable test-kit и опубликовать пересобранные артефакты всех шести
платформ штатным автоматическим workflow.

**Сделано.** Парсер принимает оба строгих варианта: чётную строку hex без
разделителей и побайтную строку с двоеточиями. Смешанные, неполные и пустые
варианты отвергаются. Unix- и Windows-сценарии test-kit теперь передают RSA ID
именно в формате `pkcs11-tool`, а EC ID остаётся слитным, что закрепляет оба пути.
Цель `softhsm2-export` локально собрана со строгими флагами; слитный и разделённый ID доходят
до поиска слота, а неверный смешанный ID отклоняется до инициализации токена.

Push-CI `32721808548` прошёл. Portable workflow `32722049066` успешно
собрал шесть платформ, прогнал шесть независимых verifier jobs и опубликовал
`v2.7.0-portable.29` на точном SHA `6845883`. В логах всех шести verifier jobs есть
`[UTIL] PASS`; запакованные Unix/Windows-сценарии везде содержат проверку
разделённого ID. С релиза заново скачаны 12 ZIP и `SHA256SUMS`: 12/12 хешей
совпали, в каждом ZIP есть `softhsm2-export`, вложенных ZIP нет, архитектуры шести
бинарных вариантов совпадают с именами пакетов.

**Дальше.** Вернуться к реальной трассе расшифрования Рутокен Плагина.

## 2026-08-22 — `softhsm2-export` и portable-утилиты

**План.** По решению владельца добавить отдельную тестовую утилиту
`softhsm2-export`: выгружать RSA/ECDSA в PKCS #8 через внутреннее состояние
SoftHSM даже для явно sensitive/non-extractable объектов, без конфигурационного
флага и дополнительных подтверждений. Стандартный PKCS #11 и атрибуты объектов
не менять. Одновременно сделать `softhsm2-util` автономной в portable-архиве,
положить обе утилиты во все шесть product ZIP и соответствующие test-kit ZIP и
проверять их в сквозном сценарии. ГОСТовый экспорт пока не делать.

**Сделано.** Реализация завершена и опубликована. `softhsm2-export` статически
подключает внутреннее ядро SoftHSM и после обычного входа по User PIN кодирует
RSA или EC в PKCS #8 PEM/DER, не проверяя чувствительность и извлекаемость, но
не меняя объект. Отладочный метод компилируется только в статическое ядро этой
утилиты: в общей PKCS #11-библиотеке его символа нет, а
`C_GetAttributeValue`/`C_WrapKey` остались прежними. Выбор требует точных
типа и ID, допускает уточнение меткой, неоднозначность отвергается. ГОСТовый
тип команда не принимает.

Portable-версия `softhsm2-util` без `--module` ищет модуль сначала рядом со
своим исполняемым файлом, затем уровнем выше — это покрывает плоский product
ZIP и `bin/` в test-kit. Обе утилиты используют фиксированный пользовательский
`softhsm.conf`, игнорирующий `SOFTHSM2_CONF`; обе статически несут
OpenSSL/C++-рантайм на Linux/Windows и добавлены во все шесть product ZIP и
шесть test-kit ZIP. Сценарий test-kit проверяет автономный `util`, принудительный
экспорт существующего sensitive/non-extractable RSA, импорт таким же образом
защищённого P-256 EC и его принудительный экспорт. Push-CI получил короткий
аналогичный гейт.

Локально обе цели собраны с обязательными строгими флагами. На отдельном
пользовательском хранилище `softhsm2-util` автономно нашёл модуль и импортировал
RSA/P-256 EC с `CKA_SENSITIVE=true`, `CKA_EXTRACTABLE=false`; экспорт в PEM и
DER прошёл `openssl pkey -check`, публичные части обоих исходных и выгруженных
ключей совпали, права файлов на Linux — `0600`. Проверены оба расположения
модуля: рядом с `util` и уровнем выше. Shared-модуль не экспортирует внутренний
отладочный символ. Полный `ctest` локально не запустился: в среде нет CppUnit;
проект и раньше осознанно не включает юнит-тесты в CI.

Первый push-CI `32564451955` остановился до запуска утилит: job внутри
контейнера выполняла новый шаг системным `/bin/sh`, а тот не поддерживает
`set -o pipefail`. Сам код и архив к этому моменту уже успешно собрались.
Шагу явно назначен `shell: bash`; это отдельный fix-forward коммит.

Повторный push-CI `32564689287` прошёл, но первый полный portable-прогон
`32564818665` нашёл отдельный MSVC-дефект: экспортёр был связан со статическим
ядром, а объявление `C_GetFunctionList` ожидало DLL import symbol. Для цели
экспортёра включён штатный `CRYPTOKI_STATIC`; исправление `3e60d1b` прошло
push-CI `32565791180` и полный portable-прогон `32565915712`: 6/6 сборок,
6/6 независимых verifier jobs и публикация успешны. Во всех шести verifier
логах есть `PASS` автономного `util` и принудительного RSA/ECDSA PKCS #8.

Автоматически опубликован `v2.7.0-portable.28` на точном SHA `3e60d1b`.
С релиза заново скачаны 12 ZIP и `SHA256SUMS`: совпали 12/12 хешей. Во всех
product/test-kit архивах обе утилиты лежат в ожидаемых местах, вложенных ZIP
нет; архитектуры ELF, PE и universal Mach-O совпадают с именами платформ.

**Дальше.** После ручной проверки владельцем вернуться к реальной трассе
расшифрования Рутокен Плагина после уже реализованного KEG. ГОСТовый экспорт
остаётся отдельной будущей задачей.

## 2026-08-21 — умолчание `CKA_EXTRACTABLE` и починка метёлки

**План.** Две находки владельца: производный VKO-ключ выходит нечитаемым, и в
вендорском диапазоне не хватает `0x80003010`, `0x8000800C`, `0x8000800F`.

**Сделано.**

- **`CKA_EXTRACTABLE` по умолчанию `true`** — для форка целиком, оба профиля.
  Разбор показал, что дело не в `CKA_SENSITIVE` (он уже `false`), а в
  `CKA_EXTRACTABLE`, и что общая ветка `C_DeriveKey` эти два атрибута от
  базового ключа не наследует. Объём выбрал владелец — все ключи, включая
  приватные; расхождение с настоящим устройством, которое это создаёт,
  разобрано в `JOURNAL`. Гейт `core-behaviour` проверяет обе стороны: молчаливый
  шаблон даёт читаемый ключ, явная просьба о нечитаемом — нечитаемый, и обратно
  он не поворачивается.
- **Метёлка в утилите чинена по-настоящему.** Прошлая версия обходила
  `0x80003000..0x80003012` целиком, но в диапазоне `0x8000800x` спрашивала
  только два адреса, которые уже знала, — поэтому `0x8000800C` и `0x8000800F`
  не спрашивались никогда. Теперь обходятся два полных диапазона по 32 адреса,
  а список имён отделён от списка того, что спрашивать.
- **Утилита научилась мерить умолчания устройства.** Второй аргумент — PIN
  пользователя; с ним она логинится и печатает по каждому ключу
  `CKA_SENSITIVE`, `CKA_EXTRACTABLE`, `CKA_ALWAYS_SENSITIVE`,
  `CKA_NEVER_EXTRACTABLE` и читается ли `CKA_VALUE`. Шаблон плагина про эти
  атрибуты молчит, значит то, что стоит у ключей на устройстве, и есть его
  умолчание — измеренное, а не выведенное из поведения ПО.

**Про `0x80003010`:** не добавлен намеренно — устройство его тоже не отдаёт,
это видно в присланном владельцем выводе. Про `0x8000800C` и `0x8000800F`
данных нет вовсе, они ждут нового прогона утилиты.

**Дальше.** Прогон утилиты с PIN, затем этап 7 — юнит-тесты на ГОСТ 2012.

## 2026-08-21 — автоматическая публикация значимых изменений

**План.** Владелец больше не хочет вручную запускать публичный релиз после
каждого значимого изменения. Научить уже существующий автоматический
`workflow_run` после шести зелёных verifier jobs сам выбирать следующий
`v2.7.0-portable.N` и публиковать его. Чистые изменения проектной документации
не считать новой версией продукта. Ручной запуск с явно заданным тегом
сохранить как аварийный путь.

**Сделано.** Автоматическая джоба публикации теперь после двенадцати успешных
сборочных и проверочных джоб сравнивает проверенный коммит с последним
portable-тегом. Если diff не ограничен проектной документацией, она выбирает
максимальный существующий номер `N`, публикует `N + 1` и никогда не затирает
автоматически выбранный тег. Чистый docs-diff заканчивается успешно без нового
тега. Ручной запуск остался: он требует явный тег правильного формата и только
в этом режиме разрешает обновить существующий релиз.

Первый живой прогон дошёл до новой джобы после 6/6 сборок и 6/6 проверок, но
упал при выборе последнего тега: `head -n 1` под обязательным `pipefail`
закрывал pipe досрочно, а `git tag` завершался по `SIGPIPE`. Заменено на
`sed -n '1p'`, который читает поток до конца; локальная проверка повторяет
реальные shell-флаги Actions.

Повторный прогон `32494259369` снова успешно прошёл 6/6 сборок и 6/6 проверок
и подтвердил исправление конвейера, но выявил следующую границу: release job
получала только shallow checkout текущего коммита, поэтому
`git merge-base --is-ancestor` не видел цепочку от последнего portable-тега.
Для этой job включён `fetch-depth: 0`; сравнение значимых изменений теперь
выполняется на полной истории.

Третий живой прогон `32497429071` на `eaf42cc` прошёл полностью: 6/6 сборок,
6/6 независимых verifier jobs и публикация. Workflow сам выбрал и создал
`v2.7.0-portable.26`. С GitHub заново скачаны 12 ZIP и `SHA256SUMS`; все
12/12 контрольных сумм совпали, тег указывает точно на проверенный коммит и
GitHub показывает релиз как Latest.

Отрицательный путь закреплён прогоном `32500403362` на чистом docs-коммите
`a7ed597`: снова прошли 6/6 сборок и 6/6 verifier jobs, release job явно
сообщила `publication skipped`, все шаги подготовки assets и публикации были
пропущены. Latest остался `.26`, тег `.27` отсутствует.

**Дальше.** Вернуться к реальной трассе расшифрования Рутокен Плагина.

## 2026-08-21 — VKO/KEG для Рутокен Плагина

**План.** Реализовать только первый недостающий шаг расшифрования:
`C_DeriveKey` для фактического вызова плагина. Сначала сверить по первичным
источникам механизм, ABI параметров и тип результата, затем сделать VKO/KEG
на Botan, проверить известным результатом и сквозным вызовом PKCS #11.
`C_UnwrapKey`, `C_Encrypt` и `C_Decrypt` не трогать — владелец оставил их на
следующий заход.

**Сделано.** По официальным заголовкам и примерам Рутокен подтверждено, что
вызов `0xD4321039` — это `CKM_GOST_KEG` с обычной
`CK_ECDH1_DERIVE_PARAMS`, а результат — 64-байтовый
`CKK_MAGMA_TWIN_KEY`. Добавлены строгая проверка ABI, Botan-реализация
VKO-2012/256 и `KDF_TREE_HMAC_Streebog256`, создание временного секретного
объекта и очистка ключевого материала. Сквозной тест импортирует независимый
вектор, сверяет все 64 байта результата, повторяет трёхатрибутный шаблон
плагина и проверяет отказ на неподдерживаемом KDF. Локально полностью прошли
сборка с OpenSSL 3.5.7 и минимальным Botan 2.19.5, `prepare` и
`rutoken-profile`.

Отдельная совместимостная ловушка закрыта в коде: Botan 2.19.5 ошибочно
сопоставляет OID TC26 256 paramSet A со старой CryptoPro-A. Для этого OID
строится настоящая cofactor-4 группа TC26 явно, а вся арифметика остаётся в
Botan.

**Дальше.** Повторить реальное расшифрование с `pkcs11-spy`: после успешного
KEG снять следующий вызов. `C_UnwrapKey`, `C_Encrypt` и `C_Decrypt` до этой
трассы не делать.

## 2026-08-21 — вендорские атрибуты сверены с устройством целиком

**План.** Досдать последний пункт третьего отчёта — три вендорских атрибута,
которых, по отчёту, не хватало Центру управления. Значения снять с устройства,
а не выдумать.

**Сделано.** Владелец прогнал доработанную `rutoken-reference-dump` на
эталонном Рутокене. Метёлка по всему диапазону вместо трёх адресов из отчёта
перевернула задачу: этих трёх атрибутов (`0x80003010`, `0x80000009`,
`0x80003304`) **нет и у устройства**, добавлять их нельзя — это было бы
расхождение с эталоном, а не совместимость. Зато нашлось девять атрибутов,
которых не было у нас и о которых никто не спрашивал; они добавлены, и объект
дорос с одиннадцати значений до двадцати. Отказ по `0x80003010` закреплён в
гейте отдельной проверкой: он выглядит как наша недоделка, и его будет тянуть
«починить». `C_EX_GetTokenName` сверена с устройством по всем трём формам —
совпала, правки не потребовалось. Попутно закрыт отложенный пункт 5
предыдущего отчёта: работы там нет.

**Дальше.** Этап 7 — юнит-тесты на ГОСТ 2012. Отдельно ждёт VKO,
`docs/HANDOFF-VKO.md`.

## 2026-08-20 — правки по третьему отчёту: даты, события слотов, имя токена

**План.** Владелец прислал третий отчёт — трассировку трёх потребителей
библиотеки: Рутокен Плагин, `ra.rutoken.ru` и Центр управления Рутокен. Из
семи пунктов он назначил четыре: починить атрибуты, которые хранятся
зашифрованными; сделать `C_WaitForSlotEvent` — причём для форка целиком, а не
только для профиля; VKO не делать, а написать по нему передачу другому агенту;
шифрование не делать.

**Сделано.**

- **`CKA_START_DATE` / `CKA_END_DATE` на приватных объектах.** Оба
  переопределения `updateAttr` клали значение мимо шифрующего базового метода,
  и любое чтение потом падало с `CKR_GENERAL_ERROR`. Теперь пишут через базовый
  метод. Хранилища, где даты уже лежат открытым текстом, продолжают читаться:
  шифртекст короче `CK_DATE` не бывает, так что старое значение опознаётся по
  длине без догадок. Дефект апстримный, воспроизведён с выключенным профилем.
- **Блокирующий `C_WaitForSlotEvent`** для форка целиком. Ожидание на
  `std::condition_variable` в состоянии, которое переживает `C_Finalize`;
  событие поднимается там, где оно в SoftHSM действительно есть, — при
  `C_InitToken` на запасном слоте. Подробности в `JOURNAL`.
- **`C_EX_GetTokenName`** — двухпроходно, при включённом профиле. Центр
  управления зовёт её сразу после `C_EX_GetTokenInfoExtended` и при отказе
  закрывает сессию.
- **`docs/HANDOFF-VKO.md`** — передача задачи по VKO: что именно запрашивает
  плагин, где отказ, чего не угадывать, чем проверять, как воспроизвести.
- **Сквозной гейт** получил третий режим, `core-behaviour`: он гоняется с
  выключенным профилем, потому что проверяет не Рутокен, а обычный PKCS #11.
  Даты, события слотов, и — через `P11_TEST_STORE_DIR` — то, что дата
  приватного ключа не лежит в файлах хранилища открытым текстом. Последнее
  оказалось единственным способом поймать дефект: миграционная поблажка
  маскирует его через API. Проверено отрицательным прогоном.
- **`rutoken-reference-dump`** дополнена: сплошной обход вендорских атрибутов
  `0x80003000`..`0x80003012`, `0x8000800D/E` по одному с запросом размера,
  вендорские атрибуты сертификата и `C_EX_GetTokenName`. Собрана под Windows
  x64 для владельца.

**Дальше.** Значения `0x80003010`, `0x8000800E` и `0x80000009` с устройства,
затем этап 7 — юнит-тесты на ГОСТ 2012. VKO — отдельной задачей.

## 2026-08-20 — объект аппаратной особенности (P1-A)

**План.** Владелец снял утилитой одиннадцать атрибутов
`CKO_HW_FEATURE` / `CKH_VENDOR_TOKEN_INFO` с эталонного устройства. Отдавать
из профиля ровно это — иначе пять методов Рутокен Плагина отвечают ошибкой.

**Сделано.** Объект синтезируется, пока профиль включён: находится поиском по
`CKA_CLASS` и `CKA_HW_FEATURE_TYPE` (и пустым шаблоном, как на устройстве),
отдаёт одиннадцать значений устройства, читается без входа по PIN, не
меняется и не удаляется. Длины совпали с устройством: булевы — один байт,
остальные — `sizeof(CK_ULONG)`, четыре байта на Windows. Вопрос про
недокументированный `0x8000800D` закрыт: устройство отдаёт один байт со
значением 0. Гейт `rutoken-profile` дополнен проверкой — она спрашивает так
же, как плагин (один вызов, буферы заранее), включая восьмибайтовый буфер под
не-булев атрибут; проверено отрицательным прогоном: подменённое значение гейт
валит. Названия `TOKEN_INTERFACE_NFC/BLUETOOTH/ISO` из `rutoken.h` убраны —
они были догадкой, и устройство её не подтверждает: в маске `0x21` второй бит
ими не объясняется.

Попутно закрыта дыра в пуш-CI: он гонял гейт только на свежем хранилище, а на
неинициализированном токене SoftHSM не открывает сессию, так что новая
проверка там не запускалась бы вовсе. Добавлен второй прогон на
инициализированном токене с меткой — полсекунды работы, и заодно ветка правила
о метке перестала быть только релизной.

**Дальше.** Этап 7 — юнит-тесты на ГОСТ 2012.

## 2026-08-20 — правки по отчёту «Рутокен Плагин против профиля»

**План.** Закрыть из отчёта владельца два пункта, которые ломают плагин
(P1-B и P1-C), уточнить кодек серийника, дописать документацию и научить
утилиту снятия эталона читать объект аппаратной особенности.

**Сделано.** `C_DigestInit` принимает DER-OID набора параметров
`1.2.643.7.1.1.2.2`; `C_GenerateKeyPair` подставляет этот же набор, когда
`CKA_GOSTR3411_PARAMS` не прислан, и принимает вендорские атрибуты ключей
`0x80002000`..`0x80002003`. Кодек серийника переведён с BCD на hex. Утилита
`rutoken-reference-dump` дополнена дампом `CKO_HW_FEATURE` /
`CKH_VENDOR_TOKEN_INFO` и собрана под Windows x64 для владельца. Пункт 6
отчёта проверен: тег `0.27.1-portable.3` форка OpenSC уже содержит все 34
`C_EX_`-обёртки `pkcs11-spy`.

**Дальше.** Показания устройства по объекту аппаратной особенности (P1-A) —
см. запись выше.

## 2026-08-14 — полная сборка продукта после каждого изменения в транке

**План.** Владелец хочет, чтобы портируемый продукт собирался целиком после
любого изменения в `main`, автоматически, следом за зелёным основным CI.

**Сделано.** Проверил историю: `portable-release.yml` был
`workflow_dispatch`-только во всех 27 своих ревизиях, с самого первого коммита
— автозапуска не было никогда и ничего не отключалось. Добавлен триггер
`workflow_run` на успешное завершение `CI` в `main`. Автоматический прогон
собирает и проверяет все шесть платформ и на этом останавливается; публикует
релиз по-прежнему только ручной запуск с тегом. Во всех тринадцати `checkout`
проставлен `ref: github.event.workflow_run.head_sha` — иначе собирался бы не
тот коммит. Новый автоматический прогон вытесняет предыдущий.

**Дальше.** Этап 7 — юнит-тесты на ГОСТ 2012.

## 2026-08-14 — пуш-CI начал проверять профиль

**План.** Владелец спросил, почему после правок в коде не пересобрался
портируемый релиз. Разобраться и закрыть дыру.

**Сделано.** Релизный workflow пушем не запускается по устройству — у него
только `workflow_dispatch` с тегом; это не поломка. Но выяснилось худшее:
`ci.yml` вообще не собирал `portable-token-e2e.cpp` и не гонял гейт
`rutoken-profile`, так что весь профиль и всё расширение `C_EX_*` проверялись
только при выпуске релиза. По решению владельца `ci.yml` расширен: клиент
собирается теми же флагами, что и в тест-ките, и гейт прогоняется на свежем
хранилище. Последовательность проверена локально сборкой GCC 9 — той же
версии, что в контейнере `ubuntu:20.04` у раннера.

**Дальше.** Этап 7 — юнит-тесты на ГОСТ 2012.

## 2026-08-14 — расширение Рутокена `C_EX_*`

**План.** Экспортировать вендорскую таблицу расширения: часть ПО опознаёт
библиотеку как рутокеновскую по наличию `C_EX_GetFunctionListExtended`.

**Сделано.** Из Rutoken SDK v.2026 вытянуты заголовки расширения (по HTTP
range-запросам к zip, без скачивания 485 МБ). ABI объявлен своими словами в
`src/lib/pkcs11/rutoken.h`; заголовки Aktiv в репозиторий не копируются.
Реализованы `C_EX_GetFunctionListExtended` (работает всегда, как
`C_GetFunctionList`) и `C_EX_GetTokenInfoExtended` (при включённом профиле),
остальные 32 — заглушки с `CKR_FUNCTION_NOT_SUPPORTED`. Описание всех 34
функций — в `docs/RUTOKEN-EXTENSIONS.md`. Сквозной гейт `rutoken-profile`
проверяет символ, полноту таблицы, согласие серийника с `C_GetTokenInfo` и
поведение заглушек. Добавлен `tests/portable/rutoken-reference-dump.c` —
снимать эталонные значения с реального устройства.

**Дальше.** Заменить неснятые константы `CK_TOKEN_INFO_EXTENDED` показаниями
устройства, затем этап 7 — юнит-тесты на ГОСТ 2012.

## 2026-08-13 — профиль `FAKE_RUTOKEN_ECP` сведён с эталонным устройством

**План.** Довести представление профиля до того, что показывает реальный
Рутокен ECP: некоторое ПО ругалось на совместимость.

**Сделано.** Константы `C_GetSlotInfo` и `C_GetTokenInfo` выверены по сырому
выводу владельца с устройства. Список механизмов заменён на список устройства
целиком — все 70, в его порядке, с его размерами ключей и флагами, включая
нереализованные (решение владельца, см. `AGENTS.md`); вызов нереализованного
даёт `CKR_MECHANISM_INVALID`. Метка: пустая — как у устройства, заполненная —
как требует стандарт. Недостающие идентификаторы ТК-26 добавлены в
`pkcs11.h`. Сквозной гейт `rutoken-profile` обновлён: он теперь сверяет весь
список с эталоном поэлементно и проверяет обе ветки правила о метке.

**Дальше.** Этап 7 — юнит-тесты на ГОСТ 2012.

## 2026-08-13 — этап 6: проверка на стенде

**План.** Проверить портируемые сборки Windows на живой машине.

**Сделано.** Владелец прогнал x86, x64 и ARM64 на Windows 11 ARM в Parallels —
все три работают, и тест-китом, и вручную. Найденный по дороге отказ
`pkcs11-tool` с относительным путём к модулю оказался посторонними копиями
`softhsm2.dll` в системных каталогах, а не дефектом проекта. Попутно укреплён
поиск профиля пользователя на Windows в `get_user_path()`; проверен на Linux и
на Windows x64.

**Дальше.** Этап 7 — юнит-тесты на ГОСТ 2012.

## 2026-08-12 — этап 5: правила и проектные документы

**План.** Проверить репозиторий на соответствие каноническим правилам,
подсветить расхождения, завести отсутствующие документы.

**Сделано.**

- Проведён аудит 38 коммитов форка (`1f65ddd`…`006c944`, 1–11 августа) против
  канонического `AGENTS.md`.
- Найдено: в репозитории нет ни `AGENTS.md`, ни одного из шести проектных
  документов; 21 коммит из 38 не по Conventional Commits и ни у одного из 38
  нет тела; юнит-тесты не запускаются в CI; у кода ГОСТ 2012 нет юнит-тестов;
  не записаны принятые решения; `ci.yml` на вершине отменён.
- Проверено и нарушений не найдено: секретов и вендорских бинарников в git нет,
  сторонние исходники пиннятся по SHA-256, UTF-8 и LF выдержаны,
  `git diff --check` чист, PR-ов нет, вся работа шла в `main`, портируемое
  поведение изолировано флагом `ENABLE_PORTABLE` (по умолчанию OFF), так что
  обычная сборка не затронута.
- Владелец разобрал находки и санкционировал спорные решения (см. `JOURNAL`).
- Канонический `AGENTS.md` скопирован в корень; к нему дописано приложение по
  проекту. Заведены `HANDOFF.md` и пять документов в `docs/`.

**Дальше.** Подтвердить у владельца этапы 6 и 7 дорожной карты. Юнит-тесты на
ГОСТ 2012 отложены в этап 7 по его решению.
