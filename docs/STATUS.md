# STATUS — состояние проекта

Обновлено: 2026-08-12. Транк: `main`, вершина `f7908e7`.

## Версия

`2.7.0` (`CMakeLists.txt`, `configure.ac`) — унаследована от апстрима, работой
форка не двигается. Последний релиз: тег `v2.7.0-portable.23` на `006c944`.

## Что работает

- Портируемые сборки для шести платформ: Linux x64/ARM64, Windows x86/x64/ARM64,
  macOS universal. OpenSSL слинкован статически; на Linux и Windows статически
  подключён и C/C++-рантайм, на macOS используется системный libc++.
- Единая пользовательская конфигурация: `~/softhsm/softhsm.conf`
  (`%USERPROFILE%\softhsm\softhsm.conf`). Портируемый модуль игнорирует
  `SOFTHSM2_CONF`, соседние файлы и системные пути.
- ГОСТ 2012: Стрибог-256, генерация ключевой пары и подпись
  ГОСТ Р 34.10-2012/256, механизмы ТК-26.
- Профиль `FAKE_RUTOKEN_ECP` (по умолчанию выключен): подменяет представление
  библиотеки, слота и токена и список механизмов; хранилище объектов и
  криптография не меняются.
- Тест-киты: распаковать и запустить `run-test.sh` / `run-test.cmd` без
  аргументов.
- Релизный workflow: шесть сборочных джоб, шесть проверочных на чистых раннерах,
  публикация только после того, как прошли все шесть.

## Что не проверяется

- **Юнит-тесты не запускаются в CI ни одним workflow.** Цели ctest в проекте
  зарегистрированы и собираются локально, но `ci.yml` их не вызывает. Так
  решено осознанно (см. «Принятые решения» в `AGENTS.md`), однако практический
  вывод остаётся: правка в `src/lib/**/test/**` уезжает в транк непроверенной.
- **У кода ГОСТ 2012 нет юнит-тестов.** `BotanGOST2012Signer`,
  `BotanGOST2012KeyGenerator`, `BotanStreebog256` покрыты только сквозным
  тестом `tests/portable/portable-token-e2e.cpp`, который живёт в релизном
  workflow, а не в `ci.yml`.
- **Профиль `FAKE_RUTOKEN_ECP` не проверялся на настоящем ПО под Рутокен.**
  Проверено только то, что подменяется представление токена и что хранилище от
  этого не меняется. Совместимость с реальным клиентским софтом — гипотеза.

## Ручная проверка владельца (2026-08-12)

Стенд: виртуальная Windows 11 ARM в Parallels на MacBook Air M5. Проверены
сборки x86, x64 и ARM64.

| Сборка | Тест-кит | `pkcs11-tool` вручную |
| --- | --- | --- |
| Windows x86 | проходит | работает |
| Windows ARM64 | проходит | работает |
| Windows x64 | проходит | **ошибка 5** |

## Что сломано

**Windows x64: ручной запуск `pkcs11-tool` падает на `C_Initialize` с
`CKR_GENERAL_ERROR` (`0x5`).** Та же команда внутри тест-кита на той же сборке
в том же каталоге проходит. Из четырёх достижимых веток `CKR_GENERAL_ERROR` в
`C_Initialize` три логируют `ERROR_MSG` и упёрлись бы в конфигурацию, которая
у владельца общая на три архитектуры и валидна. Остаётся
`objectStore->isValid()`, то есть работа с `C:\Users\m\softhsm\tokens`.
Первопричина не подтверждена — нужен лог. Разбор в `JOURNAL`, шаг в `HANDOFF`.

**Фатальный отказ `C_Initialize` не виден в логах.** Ветка
`objectStore->isValid()` и оба случая внутри `ObjectStore` логируются уровнем
`WARNING`, а `log.level = ERROR` из нашей же выдаваемой конфигурации его
глушит. Пользователь получает `0x5` и пустоту. Чинится независимо от
первопричины.

Запуск `ci.yml` на вершине `006c944` завершился со статусом `cancelled` —
отменён через пять секунд на шаге сборки. Красным транк это не делает, но
зелёным этот workflow вершину не подтверждает. Запуск `portable-release.yml`
на том же коммите прошёл успешно, включая все шесть проверок на чистых
раннерах.

## Где остановились

Этап 5 закрыт. Идёт этап 6 — проверка на реальном стенде; разбирается дефект
Windows x64. Подробности в `HANDOFF.md`.
