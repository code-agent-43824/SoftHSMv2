# JOURNAL — что узнали

Новые записи сверху. Гипотеза → что сделали → что показала проверка → вывод.
Неудачные попытки не удаляются: каждая закрывает гипотезу, к которой иначе
вернутся.

## 2026-08-13 — поиск публичных сведений о дефекте

**Гипотеза.** Отказ x64 под эмуляцией — известный дефект Prism, о котором уже
писали.

**Что сделали.** Поиск по вебу: сочетания «Windows on ARM», «Prism», «x64
emulation», «LoadLibrary», «относительный путь», «pkcs11-tool
CKR_GENERAL_ERROR», «Parallels Windows 11 ARM x64». Плюс чтение
первоисточника — страницы Microsoft «How emulation works on Arm».

**Что показала проверка.**

- **Ровно нашего симптома в публичных источниках нет.** Ни одного сообщения о
  том, что относительный путь к DLL ломает загрузку или инициализацию под
  эмуляцией x64.
- **Microsoft никаких отличий по этой части не документирует.** В официальном
  описании эмуляции сказано, что для x64 нет слоя WOW64 и нет перенаправления
  файловой системы и реестра: системные библиотеки собраны как ARM64X и
  грузятся в оба типа процессов из одного места. Про загрузку DLL,
  относительные пути, текущий каталог и CRT — ничего.
- **Общие отличия эмуляции всё же есть**, и они по нашей теме: у эмулируемых
  приложений отличается `PROCESSOR_ARCHITECTURE`, и отмечается, что
  пользовательский `PATH` может не доходить до дочерних процессов. То есть
  окружение под эмуляцией ведёт себя не полностью как нативное.
- **У связки Parallels и эмуляции есть собственный подтверждённый дефект:**
  `IsWow64Process2` не отличает эмулируемый x64-процесс в Windows 11 ARM64 под
  Parallels; Parallels подтвердила и ищет решение. Наш симптом другой, но это
  доказывает, что дефекты в этом слое существуют и не всегда лежат в Prism.

**Вывод.** Публичного описания нет, и назвать причину дефектом Prism пока
нельзя: стенд владельца — это Prism **внутри** Parallels, то есть два
кандидата, а не один, и ветка отказа до сих пор не названа. Это остаётся
гипотезой до прогона диагностической сборки на стенде.

## 2026-08-13 — прибор починен; нативный Windows x64 подтверждён чистым

**Гипотеза.** Диагностическое логирование на Windows молчит из-за того, как
определения доходят до компилятора.

**Что показала проверка.** Разложили цепочку по звеньям и нашли обрыв:

| Звено | Состояние |
| --- | --- |
| `DEBUG_LOG_STDERR` в `<PreprocessorDefinitions>` цели `softhsm_common` | есть |
| `log.obj` — тот самый файл (строки `__FILE__` на месте) | да |
| маркер и `getenv` в `log.obj` | **нет** |

То есть определение объявлено у цели, куда компилируется `log.cpp`, но до
самого компилятора не доходит. Разбираться в этом внутри MSBuild — не тот
дефект, который расследуется, поэтому условную компиляцию убрали совсем.

**Как починили.** Диагностический канал больше не зависит ни от одного
определения: он всегда скомпилирован и включается на ходу переменной
`SOFTHSM_DIAG_LOG`, пишет и в файл, и в `stderr`. Работает **до** фильтра
уровня, поэтому фатальный отказ `C_Initialize` виден при любом `log.level` —
включая наш дефолтный `ERROR`, который глушил `WARNING`-ветки. Источник
сообщения печатает сам, без `SOFTHSM_LOG_FILE_AND_LINE`.

**Подтверждение на раннере** (прогон 31690076860):

```
marker in log object            : True
marker SOFTHSM_DIAG_LOG in dll  : True
forced-failure rv line          : C_Initialize rv = 0x5
forced-failure logged to stderr : True
forced-failure logged to file   : True
diag.log line count             : 36
```

**Нативный Windows x64 чист: 15 случаев из 15.** Пять форм пути к модулю —
абсолютный, `.\softhsm2.dll`, голое имя, относительный из родительского
каталога, с прямыми слэшами — против `-I`, `-T` и `-M`. Ни одного отказа.
`-M` (`C_GetMechanismList` и `C_GetMechanismInfo` по каждому механизму) до
этого не проверялся вообще ничем.

**Собственные ошибки этого захода, чтобы не повторять.** Искали объектник под
именем `log.cpp.obj` — так именует Ninja, а MSBuild именует `log.obj`; из-за
этого один прогон отчитался пустотой. И при вырезании ненужных ворот
python-правка удалила заодно строку `cmake --build`, стоявшую между
маркерами, — прогон дошёл до проверки объектника без сборки. Отсюда правило:
после автоматической правки скрипта сверять целостность ключевых шагов, это
двадцать секунд против двадцати минут прогона.

## 2026-08-13 — дефект локализован до эмуляции; диагностический прибор не работает на Windows

**Что сделали.** Шесть прогонов одноразового workflow на ветке
`claude/diag-windows-x64-module-path`: сборка Windows x64 с
`DEBUG_LOG_STDERR`, установка OpenSC на раннер и прогон `pkcs11-tool -I` по
всем формам пути к модулю, плюс собственный загрузчик на MSVC.

**Что показала проверка.**

- **На нативном x64-раннере дефект не воспроизводится.** Все пять форм пути —
  абсолютный, `.\softhsm2.dll`, голое имя, относительный из родительского
  каталога, с прямыми слэшами — дают `exit = 0` и настоящую информацию о
  токене.
- **Собственный загрузчик работает** и печатает разрешённый путь модуля,
  текущий каталог и окружение глазами CRT процесса. На раннере он тоже не
  воспроизводит отказ.
- **Диагностический прибор на Windows молчит.** Итоговая сводка последнего
  прогона: определение `DEBUG_LOG_STDERR` присутствует в
  `softhsm_common.vcxproj` — цели, куда компилируется `log.cpp`, — но строки
  `SOFTHSM_DIAG_LOG` в готовой DLL нет, и принудительный отказ
  (`USERPROFILE` в несуществующий диск) даёт `C_Initialize rv = 0x5` без
  единого сообщения ни в консоли, ни в файле.
- **На Linux та же цепочка работает.** `CMAKE_CXX_FLAGS` доносит определения
  до `log.cpp`, маркер в `.so` есть, оба канала печатают три строки с файлом и
  номером строки. Значит теряется что-то между `.vcxproj` и объектным кодом
  именно у MSVC.

**Невалидная проверка, из которой едва не сделали вывод.** Первая локальная
проверка `CMAKE_CXX_FLAGS` показала отсутствие маркера — но она собиралась уже
после переключения на `main`, где правки в `log.cpp` нет. Пересборка на нужной
ветке дала маркер. Вывод «`CMAKE_CXX_FLAGS` не работает» был бы ошибочным.

**Итог по дефекту.** Сводная картина:

| Стенд | x64 + относительный путь |
| --- | --- |
| Linux x86_64 | работает |
| Windows x64 нативный (раннер) | работает |
| Windows x64 под эмуляцией (стенд владельца) | `CKR_GENERAL_ERROR` |
| Windows ARM64 нативный (стенд владельца) | работает |
| Windows x86 под эмуляцией (стенд владельца) | работает |

Ломается единственная клетка — 64-битный код под эмуляцией Prism. Причина не
установлена, обходной путь есть: абсолютный путь к модулю.

**Остановились здесь.** Шесть прогонов ушло на прибор, а не на дефект. Дальше
нужно решение владельца: чинить прибор, ограничиться обходным путём или
копать на стенде отладчиком.

## 2026-08-12 — попытка воспроизвести дефект на Linux

**Гипотеза.** Логика поиска конфигурации в `SimpleConfigLoader.cpp` общая для
платформ. Если дефект в ней, относительный путь к модулю сломает
`C_Initialize` и на Linux.

**Что сделали.** Windows, MSVC и wine в окружении агента нет, поэтому собрали
портируемый модуль локально: те же флаги CMake, что в `build-windows.ps1`, но
без Botan и ГОСТ — их в окружении нет, а к разбору конфигурации они отношения
не имеют. Вместо `pkcs11-tool` написали минимальный загрузчик: `dlopen`,
`C_GetFunctionList`, `C_Initialize`, `C_GetInfo`, `C_GetSlotList`.

**Что показала проверка.**

- **Дефект не воспроизводится.** `./libsofthsm2.so` и абсолютный путь дают
  одинаковый результат: `C_Initialize` возвращает `CKR_OK`, слот находится,
  конфигурация и хранилище создаются в `$HOME/softhsm`. Значит механизм
  Windows-специфичен и в кроссплатформенной логике поиска конфигурации его
  нет — это согласуется с чтением кода, где путь модуля в портируемой сборке
  не читается вовсе.
- **Зато подпись симптома воспроизведена в точности, другим способом.** Если
  заставить `getConfigPath()` вернуть `NULL` — направить `HOME` туда, где
  каталог не создать, — получается ровно то, что видит владелец:
  `C_Initialize(NULL) rv = 0x5` и **ни строчки** вывода. Это независимое
  подтверждение, что отказ сидит на шаге разбора конфигурации, а не дальше.
- **Инструмент проверен и работает.** Сборка с `DEBUG_LOG_STDERR` и
  `SOFTHSM_LOG_FILE_AND_LINE` на том же отказе печатает три строки:

  ```
  .../SimpleConfigLoader.cpp(411): Could not determine or create the per-user SoftHSM configuration path
  .../Configuration.cpp(173): Failed to load the SoftHSM configuration
  .../SoftHSM.cpp(603): Could not load the configuration
  ```

  То есть артефакт с диагностической сборки назовёт ветку так же.

**Негодная проверка, не считать за результат.** Попытка проверить ветку
«конфигурация есть, но нечитаема» через `chmod 000` ничего не показала: агент
работает от root, а root обходит проверку прав, файл был прочитан как обычно.
Эту подветку по-прежнему различит только вывод с Windows.

## 2026-08-12 — ошибка 5 на Windows x64: найдено точное условие воспроизведения

**Данные владельца.** Три проверки:

- Тест-кит в каталоге x64 **проходит** и сейчас — контрольный прогон в текущем
  состоянии, после удаления `tokens` и правки конфигурации.
- `--module` **абсолютным** путём — **работает**, в том числе на x64.
- `--module .\softhsm2.dll` — **падает** с `CKR_GENERAL_ERROR`.

На x86 и ARM64 относительный путь работает.

**Условие воспроизведения.** Windows x64, `pkcs11-tool --module` с
относительным путём к модулю → `C_Initialize` возвращает `CKR_GENERAL_ERROR`.
Абсолютный путь — работает. Это же объясняет, почему тест-кит зелёный: в
`run-test-kit.ps1` путь к модулю проходит через `Resolve-Path`, то есть всегда
абсолютный.

**Временный обходной путь для пользователей.** Передавать модуль абсолютным
путём.

**Что показала проверка кода.**

- **Единственная запись в журнале событий к делу не относится.** Это
  `Configuration.cpp(134): Missing FAKE_RUTOKEN_ECP in configuration. Using
  default value: false` от 11.08 — сообщение о старой конфигурации без этого
  ключа, а не отказ. Зато оно доказывает, что канал журнала событий у сборки с
  раннера `windows-2025` работает, и потому отсутствие сегодняшних сообщений
  значимо: отказ действительно ничего не пишет.
- **По префиксу `__FILE__` видно, чем собран модуль.** `D:\a\SoftHSMv2\...` —
  раннер `windows-2025` (x86 и x64), `C:\a\SoftHSMv2\...` — `windows-11-arm`
  (ARM64). Удобный признак при разборе чужих логов.
- **Наш код в портируемой сборке путь модуля не читает вообще.**
  `moduleFilePath()` и `getPortableConfigPath()` — единственные его читатели —
  закрыты `#ifndef SOFTHSM2_PORTABLE_USER_CONFIG`. `SimpleConfigLoader.cpp`
  компилируется ровно один раз, только в составе `softhsm_common`, и
  определение навешивается там без условий по архитектуре
  (`src/lib/common/CMakeLists.txt:28`). Дублирующей компиляции без определения
  нет. Значит механизм не в нашем поиске конфигурации, и из исходников он не
  выводится.

**Что сделали дальше.** Завели ветку `claude/diag-windows-x64-module-path` с
одноразовым workflow `diag-windows-x64.yml`: собирает только Windows x64 с
`DEBUG_LOG_STDERR` и `SOFTHSM_LOG_FILE_AND_LINE`, включаемыми через
`PORTABLE_DIAGNOSTIC_LOG=1`. Такой модуль дублирует каждое сообщение на
`stderr` до всякой конфигурации и до журнала событий, поэтому назовёт ветку
отказа сам. В `main` ветка не идёт.

## 2026-08-12 — ошибка 5 на Windows x64: два отрицательных результата

**Гипотеза.** Сообщение об отказе лежит в журнале событий Windows, а различие
между проходящим и падающим запуском — в переменных среды.
**Обе неверны.**

**Что показала проверка.**

- **В журнале событий пусто.** За последние 300 записей журнала приложений
  ровно одна от источника `SoftHSM`, от 11.08 18:22, с пустым отрендеренным
  текстом. От сегодняшних падений — ни одной. При этом запись от 11.08 попала
  в выборку, значит окно в 300 событий покрывает и сегодняшний день.
- **Причина в самом шиме.** `src/lib/win32/syslog.cpp` при неудаче
  `RegisterEventSourceA` оставляет `hEventLog == NULL` и молча выходит —
  запасного канала нет. Так что журнал событий не доказывает ни наличия, ни
  отсутствия сообщения: на этом канале диагностику строить нельзя.
- **`powershell -NoProfile` не помог** — x64 падает так же. Профиль
  пользователя и переменные среды сессии из подозреваемых выбывают.
- **Возврат именно `CKR_GENERAL_ERROR`, а не `CKR_FUNCTION_FAILED`.** Обёртка
  в `main.cpp` ловит любое исключение и отдаёт `CKR_FUNCTION_FAILED`, так что
  исключение исключено: отказ — один из шести явных возвратов в
  `SoftHSM::C_Initialize`, и каждому из них предшествует `ERROR_MSG`.
- **`SOFTHSM2_PORTABLE_USER_CONFIG` задаётся одинаково для всех архитектур** —
  `src/lib/common/CMakeLists.txt:28`, под общим `ENABLE_PORTABLE`, без
  условий по архитектуре.

**Осталось необъяснённым.** Единственное известное различие между проходящим
запуском (тест-кит) и падающим (руками) — форма пути к модулю: кит передаёт
`--module` абсолютным путём, владелец руками передаёт `.\softhsm2.dll`. Но
проход кита наблюдался до того, как владелец удалил `tokens` и правил
конфигурацию, поэтому сначала нужен контрольный прогон кита в текущем
состоянии.

## 2026-08-12 — ошибка 5 на Windows x64: отказ на разборе конфигурации

**Гипотеза (предыдущей записи).** Отказ в `objectStore->isValid()`.
**Она неверна.** Опровергнута данными владельца, см. ниже.

**Что сделали.** Владелец дописал в `C:\Users\m\softhsm\softhsm.conf` строки
`log.level = DEBUG` и `log.file = C:\Users\m\softhsm\softhsm.log`, затем
запускал `pkcs11-tool -I` вручную из каталогов x64 и ARM64 подряд, в одной и
той же сессии PowerShell.

**Что показала проверка.**

- **На x64 файл лога не появился вообще; на ARM64 появился сразу** — при одном
  и том же файле конфигурации. Лог с ARM64 содержит ровно одну строку:
  `Directory C:\Users\m\softhsm\tokens refreshed: 0 files, 0 subdirs`.
- **`setLogFile` — четвёртый шаг `C_Initialize`**, после перезагрузки
  конфигурации и `setLogLevel`. Раз файл не создан, x64-процесс до четвёртого
  шага не дошёл. Значит отказ на первом или третьем шаге, а не на хранилище:
  предыдущая версия закрыта.
- **Удаление каталога `tokens` ничего не изменило** — x64 падает и на пустом
  хранилище. Ещё одно подтверждение, что дело не в нём.
- **Правки владельца в конфигурации уцелели**, то есть `materializeUserConfig`
  файл не перезаписывал.
- **Обе архитектуры запускались из одной сессии PowerShell**, подряд, одной и
  той же командой. Окружение процесса у них общее — так что различие не в
  переменных среды сессии.
- **Тест-кит запускает PowerShell иначе** — `run-test-kit.cmd` вызывает
  `powershell -NoLogo -NoProfile -ExecutionPolicy Bypass -File run-test.ps1`.
  Ручной запуск идёт из интерактивной сессии с профилем пользователя. Это
  единственное известное различие между проходящим и падающим запуском.
- **Строка `[SCRIPT] verified reuse of pre-existing canonical user config`
  ничего не доказывает.** В `run-fresh-integration.ps1` она печатается в ветке
  «конфигурация уже была» и не проверяет, какой файл прочитал модуль;
  содержательные проверки живут только в ветке первого запуска.

**Вывод.** Отказ на первом шаге `C_Initialize` — `Configuration::reload`.
Наиболее вероятно, что `getConfigPath()` вернул `NULL`, то есть
`get_user_path()` не собрал путь к `%USERPROFILE%\softhsm\softhsm.conf`. Это
согласуется и с тем, что конфигурация не перезаписана, и с догадкой владельца
про пути. Не подтверждено: обе ветки первого шага пишут `ERROR_MSG`, а
`softLogLevel` до `setLogLevel` равен `LOG_DEBUG` (`log.cpp:51`), поэтому
сообщение гарантированно ушло в журнал событий Windows и лежит там сейчас.
Именно оно назовёт ветку и путь.

## 2026-08-12 — ошибка 5 на Windows x64: отказ в `C_Initialize`, диагностика заглушена нами самими

**Данные владельца.** Дословный отказ:

```
PS C:\Users\m\dev\softhsm-testkit-windows-x64> .\bin\pkcs11-tool.exe --module .\softhsm2.dll -I
Main C_Initialize(NULL) rv:CKR_GENERAL_ERROR
error: PKCS11 function C_Initialize failed: rv = CKR_GENERAL_ERROR (0x5)
```

Та же команда внутри тест-кита на той же сборке в том же каталоге проходит.
Все три кита брались из артефактов, `pkcs11-tool` — всегда из своей папки
своей разрядности, `FAKE_RUTOKEN_ECP = false`, конфигурация одна на все
архитектуры: `C:\Users\m\softhsm\softhsm.conf`.

**Что показала проверка.**

- **Отказ ровно в `C_Initialize`**, до всякой работы с токенами.
- **В `C_Initialize` шесть возвратов `CKR_GENERAL_ERROR`, из них достижимы
  четыре.** Проверки `SecureMemoryRegistry::i() == NULL` и
  `CryptoFactory::i() == NULL` недостижимы: обе — синглтоны на `new`, который
  `NULL` не возвращает.
- **Три достижимые ветки логируют `ERROR_MSG`** — сбой `Configuration::reload`,
  сбой `setLogLevel`, сбой `ObjectStoreToken::selectBackend`. Все три упёрлись
  бы в конфигурацию, а она у владельца общая на три архитектуры и валидна,
  плюс любая из них оставила бы запись в журнале событий Windows.
- **Четвёртая ветка — `objectStore->isValid() == false` — логирует
  `WARNING_MSG`.** А `log.cpp` фильтрует по `if (loglevel > softLogLevel)
  return;`, и при `log.level = ERROR` (`LOG_ERR` = 3) сообщение уровня
  `LOG_WARNING` = 4 отбрасывается. Значение `log.level = ERROR` стоит в
  `DEFAULT_USER_CONFIGURATION` — то есть в конфигурации, которую портируемый
  модуль пишет сам.
- **Внутри `ObjectStore` невалидность даёт ровно два случая, и оба — тоже
  `WARNING_MSG`**: не удалось создать каталог хранилища
  (`ensureStoreDirectory`) или не удалось его перечислить
  (`Directory::isValid`). Оба про один путь — `C:\Users\m\softhsm\tokens`,
  куда `directories.tokendir = tokens` разворачивается относительно каталога
  конфигурации.

**Вывод.** Отказ почти наверняка в `objectStore->isValid()`, то есть в работе
с `C:\Users\m\softhsm\tokens`. Точную из двух веток назовёт лог.

**Отдельный дефект, не зависящий от первопричины.** Фатальный отказ
`C_Initialize` сообщается уровнем `WARNING`, который глушится конфигурацией
нашей же выдачи. Пользователь портируемой сборки получает `0x5` и ноль
диагностики где бы то ни было. Это надо чинить независимо от того, чем
окажется исходная причина.

## 2026-08-12 — разбор ошибки 5 на Windows x64: что уже отсечено

**Гипотеза.** Ручной запуск `pkcs11-tool` на сборке Windows x64 падает с
ошибкой 5, хотя тест-кит на той же сборке проходит целиком. Значит, отказ
лежит в коде, который тест-кит не задевает.

**Что сделали.** Прочитали код и сценарии; воспроизвести не смогли — Windows и
ARM-стенда у агента нет, так что всё ниже получено чтением, а не запуском.

**Что показала проверка.**

- **Тест-кит трогает `pkcs11-tool` только двумя вызовами** — `-I` и `-T`
  (`tests/portable/run-test-kit.ps1`, функция `Invoke-OpenSCPkcs11Tool`). То
  есть проверены `C_Initialize`, `C_GetInfo`, `C_GetSlotList` и
  `C_GetTokenInfo`, и ничего больше. Любая ручная команда — `-M`, `-O`,
  `--login`, `--keypairgen` — идёт по коду, который автоматика не проходила
  ни разу.
- **Версия «`C_GetMechanismInfo` вернул `CKR_GENERAL_ERROR`» отпадает.** В
  `SoftHSM.cpp` восемь таких возвратов (строки 1161–1259), все срабатывают
  только если `CryptoFactory::getAsymmetricAlgorithm` вернёт `NULL`. В
  `OSSLCryptoFactory` эта функция для RSA, DSA, DH, ECDSA, ECDH, EDDSA, ML-DSA
  и ML-KEM всегда возвращает `new`-объект, а `NULL` — лишь для неизвестного
  значения перечисления. По этим веткам `0x5` получить нельзя.
- **Разницы в сборке между архитектурами нет.** `build-windows.ps1` меняет от
  архитектуры только цель OpenSSL, CPU для Botan, `-A` для CMake и ожидаемый
  тип PE-машины. Набор флагов CMake и, значит, набор возможностей модуля у
  x86, x64 и ARM64 одинаковый. Объяснять отказ разной функциональностью сборок
  нельзя.
- **Изменения формата ABI исключены.** Правка `src/lib/pkcs11/pkcs11.h` в
  форке — только восемь `#define` с идентификаторами ТК-26; `#pragma pack`
  вокруг структур не тронут.
- **На Windows диагностика по умолчанию не видна.** В конфигурации, которую
  портируемый модуль создаёт сам, есть `log.level = ERROR`, но нет `log.file`.
  При пустом `log.file` `SoftHSM::C_Initialize` оставляет вывод на `syslog`, а
  на Windows это шим `src/lib/win32/syslog.cpp`, пишущий в журнал событий
  Windows через `ReportEventA` от источника `SoftHSM`. Источник в реестре не
  зарегистрирован, поэтому запись выглядит как «описание события не найдено»,
  но текст сообщения в ней остаётся.
- **CI никогда не работает с настоящим профилем пользователя** — в
  `verify-windows-*` стоит `set "USERPROFILE=%RUNNER_TEMP%\..."`. Всё, что
  связано с правами и содержимым реального `%USERPROFILE%\softhsm`, автоматикой
  не покрыто в принципе.

**Вывод.** Причина не установлена. Отсечены: разница в возможностях сборок,
поломка ABI и ветки `CKR_GENERAL_ERROR` в `C_GetMechanismInfo`. Остаются две
живые версии, и различить их можно только дословным выводом отказа: либо
`0x5` — это `CKR_GENERAL_ERROR` из другого места, либо это Win32
`ERROR_ACCESS_DENIED` при работе с реальным `%USERPROFILE%\softhsm`, куда CI
не заглядывает. Полезное независимо от исхода: покрытие `pkcs11-tool` в
тест-ките заканчивается на `-I` и `-T`, и это стоит расширить.

## 2026-08-12 — решения владельца по итогам аудита

**Гипотеза.** Пять особенностей репозитория выглядят ошибками: вырезанная
матрица CI апстрима, профиль `FAKE_RUTOKEN_ECP`, игнорирование `SOFTHSM2_CONF`
портируемым модулем, вызов Botan-класса из `OSSLCryptoFactory` и заброшенные
апстримные документы.

**Что сделали.** Собрали факты по коду и истории и вынесли их владельцу.

**Что показала проверка.** Все пять — намеренные. Владелец подтвердил:

- Форк тупиковый. Код слишком специфичный, наверх он не пойдёт, коммиты из
  апстрима не нужны, совместимость поддерживать не требуется. Отсюда и
  урезанная матрица CI: собирать то, что мы не поставляем, незачем.
  Восстанавливать удалённые джобы не нужно.
- Апстримные документы больше не нужны, их просто игнорируем.
- `FAKE_RUTOKEN_ECP` нужен, чтобы тестировать ПО, работающее с Рутокеном, в
  случаях, когда физически вставить токен некуда — например, на облачном
  сервере. Это цель форка, а не трюк.
- Игнорирование conf-файла и переменной `SOFTHSM2_CONF` нужно для
  переносимости библиотеки.
- Применение Botan для ГОСТ 2012 — осознанное решение.
- Юнит-тесты на ГОСТ 2012 полезны, но не сейчас: переносятся в план.

**Вывод.** Решения санкционированы владельцем, а не сымпровизированы агентом.
Записаны в «Принятых решениях» приложения к `AGENTS.md`; по §8 агент их сам не
пересматривает. Следующему агенту: не поднимать эти темы заново и не
«исправлять» их.

## 2026-08-12 — юнит-тесты не исполняются в CI

**Гипотеза.** Зелёный транк означает, что тесты проходят.

**Что сделали.** Прошли поиском по всем workflow: `make check`, `ctest`,
`cppunit`, `test-suite.log`.

**Что показала проверка.** Ни одного вхождения. `ci.yml` после переработки под
портируемую сборку содержит одну джобу — сборку зависимостей и аудит архива.
Цели ctest при этом в проекте зарегистрированы (`enable_testing()`,
`add_test` в восьми каталогах `test/`) и собираются локально, а в autotools
живёт `TESTS = cryptotest`. То есть тесты существуют и исполнимы, но CI их не
вызывает. Заодно выяснилось, что форк правил
`src/lib/object_store/test/ObjectStoreTests.cpp` — файл, который с тех пор ни
разу не исполнялся автоматически.

**Вывод.** «Зелёный транк» в этом репозитории означает «портируемый архив
собирается и проходит аудит», и ничего не говорит о юнит-тестах. Прогон CI сам
по себе не подтверждает правку в тестах или в криптокоде — до восстановления
прогона проверять такие изменения локально через `ctest`.
